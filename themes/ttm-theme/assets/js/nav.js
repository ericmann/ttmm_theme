/**
 * Phone navigation toggle. The only front-end script the theme ships (SPEC §3.2 rule 6).
 * No network requests, ever.
 */
( function () {
	'use strict';
	// Phase 2 of docs/SPEC.md implements the overlay toggle.
} )();
