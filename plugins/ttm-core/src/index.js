/**
 * Editor bundle entry for ttm-core (sidebar panel, pre-publish checks, block registrations).
 * Blocks under plugins/ttm-core/blocks/<name>/ are discovered by wp-scripts via their block.json.
 * Phase 1 of docs/SPEC.md fills this in.
 */
