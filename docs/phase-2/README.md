# Phase 2 — front-page fidelity (archived)

The second Foundry flight (2026-09-21/22) made the front page match mock `2a`/`3a`
and merged as PR #11. Everything here is historical. The spec that drives the
current flight is `docs/SPEC.md`; where they disagree, `docs/SPEC.md` wins.

`SUMMARY.md` §"Spec issues" lists the corrections folded into the phase 3 spec.
