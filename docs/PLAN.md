# These Things Matter — phase 3 (inner-template fidelity) build plan
Derived from docs/SPEC.md v3.0 (commit 13686f3) on 2026-09-21. SPEC.md wins over this file.

## Decisions
- Phases → SPEC §8's six phases (0 Harness and shared chrome, 1 Article, 2 Journal, 3 Archives/search/404/page, 4 Series hub/single series/Writing, 5 Phone/wide/close-out), in order, never merged. Each phase ends with a push task that runs `npm run screenshots`, commits the PNGs under `docs/feedback/phase-3/`, pushes, and logs `Manual check: NOT VERIFIED (human)` naming the mock.
- Branch and repo hygiene → the run branch is `refine/<date>` from `main` (SPEC §7). Commit signing is intentionally off for this flight (`commit.gpgsign=false`, owner will rebase-sign); never re-enable it. `FOUNDRY_FEEDBACK.md` at the repo root is the owner's log (already gitignored): never stage, move, delete or mention it in a commit. `docs/phase-1/` and `docs/phase-2/` are history, never instructions; do not edit them.
- S1 root padding vs container → keep `theme.json` `styles.spacing.padding` and `useRootPaddingAwareAlignments: true` (core's `alignfull` break-out for the poster depends on them) **and** add `.wp-site-blocks { max-width: 1280px; margin-inline: auto; padding-inline: var(--wp--custom--gutter--desktop); box-sizing: border-box }` in `ttm.css` under a new `/* 0.2 page container */` header. Both paddings land on the same element, so nothing doubles; `container-gutter` asserts 48px / 1184px content box. The phone gutter keeps working through the existing `body { --wp--custom--gutter--desktop: var(--wp--custom--gutter--phone) }` at ≤ 720.
- S2 nav breakpoint → 720 by CSS: between 600 and 720 the theme re-hides `.wp-block-navigation__responsive-container:not(.is-menu-open)` and re-shows the open button; at ≥ 721 both buttons are `display: none` and the list is inline (rule 43). The inner masthead nav uses `overlayMenu: "mobile"`, `hasIcon: false`, so the open button reads "Menu"; the close button therefore reads core's "Close" (24px/800, top-right) — 01 §4.3's "×" glyph is not reproduced because `hasIcon` governs both buttons and markup cannot vary per viewport.
- S3 newsletter box copy → a new block-binding source `ttm/newsletter-copy` (no args) bound into the box title paragraph: "Get the next part" when `is_singular('post')` and `Query\SeriesIndex::for_post()` is non-null or `is_tax('series')`, else "The weekly issue." The copy line stays static ("Series land in the newsletter the week they publish."). One DOM node, testable in PHP; the pattern's static text is the plugin-inactive fallback.
- S4 footer → one string everywhere ("These Things Matter · © {year} Eric Mann · Built on WordPress"); unchanged.
- S5 kicker for tag/date archives → a new binding `ttm/archive-kind` (no args) bound into the archive-header kicker paragraph: `is_category()` → "Section", `is_tag()` → "Tag", `is_month()` → "Month", `is_year()` → "Year", `is_day()` → "Day", `is_author()` → "Author", `is_search()` → "Search", else "" (the paragraph is dropped, see "Empty bound blocks"). One `ttm/archive-header` pattern serves `category.html`, `category-journal.html` and `archive.html`; `search.html` has its own header group.
- S6 filter-row rule when F16 hides the row → the archive body group `.ttm-archive-body` carries `border-top: var(--ttm-rule-2)`; `.ttm-filter-row + .ttm-archive-body { border-top: 0 }` removes it when the filter row (which has its own 2px top rule and 1px bottom rule) renders directly before it. No `:has()`, no extra separator.
- S7 seed images → rule 45 in `Cli\Seeder::image()`; band angle from `Config::get('seed.image_band_angle', 30)` ⚠️ ASSUMPTION (only the Seeder reads it); tuned in P0-06.
- S8 CSS budget → `cssBudgetBytes` = 61440 ⚠️ ASSUMPTION set in P0-01 (`scripts/check-budget.mjs` and `CLAUDE.md` only); measured and settled once in P5-03.
- Colour vs a11y (phase 2 decision, extended) → any text under 18.66px that §6.9 asserts as `accent`, `neutral-500` or `neutral-600` is rendered and asserted as `accent-700` / `neutral-700` (`#9b9797` and `#7d7979` on `#f3f2f2` are 2.6:1 and 3.85:1; axe reports `color-contrast` as *serious*, which the `a11y` row forbids). Affected rows: `nav-current`, `nav-hub-current` (accent-700), `js-words` (neutral-700), `wr-chapter-num`, `ar-mostread-num`, `hub-part-num` (neutral-700), `wr-serial-status` for complete/hiatus (neutral-700), TOC numbers and the TOC heading's series name (neutral-700), the disabled pagination side (neutral-700), `box-btn` background `accent-700` (matches phase 2's `writing-btn` and the shipped `.btn-primary`). Everything else is §6.9 verbatim. Marks, segments and rules (non-text backgrounds) keep their literal colours.
- Rule 44 slug → `theme.json` `h2` is renamed `article-h2` as SPEC says. WordPress kebab-cases letter/digit boundaries, so the generated property is `--wp--preset--font-size--article-h-2`; `scripts/check-theme-json.mjs` computes generated names with the same rule (`/(?<=[a-z])(?=\d)/` → `-`) and fails on any `--wp--preset--font-size--*` / `--wp--preset--color--*` reference in `ttm.css` or `theme.json` `styles` that no slug generates. `ttm.css` and `theme.json` reference `--article-h-2`.
- Harness during the flight (rule 34 amendment vs. `verify`) → `scripts/css-coverage-allow.txt` may only contain lines of the exact form `<single class> # P<n>-<nn> pending` (no globs); `scripts/check-css-coverage.mjs` fails on any other line and on ≥ 1 line once `ALLOW_PENDING` is `false` (flipped in P5-02). Likewise `scripts/check-fixme.mjs` accepts `test.fixme(` only on a line carrying `// P<n>-<nn>` naming the owning task until P5-02 flips `ALLOW_TAGGED` to `false`. Without this, every Phase 0 task would fail `npm run lint`, which `verify` runs after every task.
- Rule 41 selectors spec → `tests/e2e/selectors.spec.mjs` (Playwright project `fidelity`) reads `themes/ttm-theme/assets/css/ttm.css` and `style.css` through the pure parser `scripts/lib/css-selectors.mjs` (`selectorsOf(cssText)` returns `{selector, inMedia}` for every rule; comma lists split; `::before/::after` stripped for querying), keeps selectors containing `.ttm-` or `.is-style-`, auto-exempts selectors containing `:hover`, `:focus`, `:focus-visible`, `:active`, `[data-state`, `.is-empty`, `.is-nocover`, `.is-textonly`, `::-webkit-scrollbar`, `.ttm-nav-open`, and rules inside `@media`, visits the screen set once per screen, runs `document.querySelectorAll(selector).length` for every selector, and fails naming each selector with zero matches across the set unless it is listed in `tests/e2e/selectors-allow.txt` (`<selector> # <reason>`). Screen set = SPEC §1 "Done" + `/` + `/category/security/?tag=wordpress` (front-page and `tag-accent` selectors must match somewhere). Lines with reason `# P<n>-<nn> pending` are removed by that phase's push task; lines with reason `# editor block style (04 §3)` may remain (SPEC rule 41 permits reasoned entries).
- Whole-row links → `Blocks\Helpers::link_rows()` on `render_block_core/group`: when the group's `className` contains `ttm-archive-row` or `ttm-journal-row` and the block has a `postId` context, the wrapper `<div …>` becomes `<a href="{permalink}" …>` (same attributes) and the closing `</div>` becomes `</a>`; the inner `core/post-title` uses `"isLink":false` so no anchor nests. Accessible name is the row's text (headline first).
- Kicker term order → `Meta\PrimaryCategory::order_terms()` on the `get_the_terms` filter: for taxonomy `category` on the front end, terms are ordered primary category first, then by `sections.order` index of each term's top-level ancestor slug, then by name. `core/post-terms` (category, `.is-style-kicker`, `separator: " · "`) therefore reads "Technology · Security" on the seeded article.
- Byline tags → `Blocks\Helpers::style_tag_terms()` on `render_block_core/post-terms`: when the block's `className` contains `is-style-tags`, every `<a` gains `class="tag tag-neutral"` and the `wp-block-post-terms__separator` spans are removed.
- Featured-image caption → `Blocks\Helpers::featured_caption()` on `render_block_core/post-featured-image`: on `is_singular()`, when the context post's thumbnail has a non-empty `wp_get_attachment_caption()`, `<figcaption class="ttm-hero__caption">{caption}</figcaption>` is inserted before the closing `</figure>`. The template drops the `aspectRatio` attribute; `ttm.css` owns 16/9 (desktop) and 4/3 full-bleed (≤ 720) on the figure, `object-fit: cover` on the img.
- Search block classes → `Blocks\Helpers::style_search()` on `render_block_core/search`: adds `input` to `.wp-block-search__input` and `btn btn-secondary` to `.wp-block-search__button`.
- Pagination → `Bindings\Sources::label_next()/label_previous()` (existing relabel filters) additionally return `<span class="wp-block-query-pagination-{next|previous} is-disabled">{plain label}</span>` when core rendered nothing for that side and the query inherits; plain labels "← Newer" / "Older →". `core/query-pagination-numbers` is removed from every template.
- Empty bound blocks → `Bindings\Sources::drop_empty_bound()` on `render_block_core/paragraph` and `render_block_core/heading`: a block whose `metadata.bindings.content.source` starts with `ttm/` and whose rendered text (`wp_strip_all_tags`, trimmed) is empty renders `''` (06 governing rule: nothing renders empty). Used by `jr-count-hidden` (`ttm/word-count` with `{"whenUnsyndicated":true}` returns `''` when the post has any syndication URL) and by `ttm/archive-kind`.
- New bindings this flight (all in `Bindings\Sources`, pure formatting in `Bindings\Values`, each with a unit test for the normal and empty value and an integration test): `ttm/newsletter-copy`, `ttm/archive-kind`, `ttm/section-label` (`{"format":"more-in"}` → "More in {primary category}" on singular; `{"format":"series-in"}` → "Series in {queried category}" on category archives; else ''), `ttm/search-summary` (→ "Results for “{q}”" or "Nothing matched “{q}”." when `found_posts` is 0; '' off search). Extended: `ttm/word-count` arg `whenUnsyndicated`; `ttm/category-count` format `journal-full` ("Full journal · {N} entries", 0 → "All →"); `ttm/meta-line`'s `tags-or-series` part joins tag names with " · " (was ", ").
- `ttm/series-list` layouts → the `layout` enum becomes `list | rail | grid-2 | grid-3 | strip` (`rows` is removed; every template and test that said `rows` is updated in the same commit — rule 46). Wrapper class `is-<layout>`. Per layout the row emits: `strip` = mark, `__title`, `__meta` (categories · count · cadence, unchanged); `rail` = mark, `__title`, `__meta` (same meta line as strip); `list` and `grid-2`/`grid-3` = mark, `__title`, `__dek`, `__meta` (fiction: "{Form} · {genre} · {cadence}"; nonfiction: categories joined " · " then cadence; empties omitted — in `grid-2` this line carries class `__categories` instead of `__meta` because the hub shows categories only), `__count` (`__parts` over `__status`). `related_limit`: when `excludeCurrent` is true and `limit` ≤ 0 the limit is `Config::get('series.related_limit', 4)`.
- `ttm/series-toc` → element classes `__num`, `__title`, `__dek`, `__date`; heading `div.ttm-cell-heading.is-rail` with `span.ttm-cell-heading__label`; in the `series` variant the heading's right side renders two anchors, `a.ttm-series-toc__series` (series name, shown ≥ 721) and `a.ttm-series-toc__hub` ("Hub →", shown ≤ 720), both to the series page; in the `chapters` variant the heading label is "{Series name} — recent chapters" (a `heading` attribute, when set, replaces the whole label), the right side is `a.ttm-cell-heading__link` "All {published}" to the series page, and rows are `ol.ttm-numbered > li.ttm-numbered__row` (grid `44px 1fr auto`) with `__num`, `__title`, `__dek`, `__date`.
- `ttm/series-featured` → element classes `__main`, `__kicker`, `__title`, `__dek`, `__buttons`, `__parts`, `__part`, `__num`, `__part-title`, `__part-dek`, `__date`, `__all`; kicker joins categories with " · "; the title tag is `h1` when `is_tax('series')`, else `h2`; new attribute `showDek` (bool, default false) renders `span.ttm-series-featured__part-dek` (the part's excerpt) for published parts only.
- `ttm/most-read` → rows become the shared numbered component: `div.ttm-cell-heading.is-rail > p.ttm-cell-heading__label` then `ol.ttm-numbered > li.ttm-numbered__row > span.ttm-numbered__num + a`.
- Nav current section → `Nav\CurrentSection` no longer marks `/series/` current on a single post that belongs to a series (SPEC §6.1.1: singles mark the post's primary category only); `/series/` and `/series/{slug}/` still mark it.
- Inner masthead → left column is `div.ttm-masthead-inner__title` (flex, baseline, gap 10) holding `core/site-title {"level":0}` (renders `<p class="wp-block-site-title"><a href="/">…</a></p>`) and `p.ttm-masthead-inner__by` "by Eric Mann". SPEC's "in one `<a>`" is not reproducible with core blocks; recorded. `parts/header-inner.html`'s header group uses `layout: default`.
- Series bar → render restructured to three grid children: `span.ttm-series-mark`, `span.ttm-series-bar__text` (containing `span.ttm-series-bar__label` "Series · ", `a.ttm-series-bar__name`, " · ", `span.ttm-series-bar__part`), `span.ttm-series-bar__right` (`span.ttm-series-bar__segments` + `a.ttm-series-bar__view`). Phone hides `__label` and `__view`; the part text stays "Part 3 of 6" (3b's "3 of 6" would need viewport-varying markup).
- Hardening WordPress seed → parts 1–3 as today; part 4 "Keys in the environment, rotated live" published, category `security`, `days_ago: 0` (a Technology part newer than part 3 would replace the front-page lead and break phase 2 rows); parts 5 "The admin with the weak password" and 6 "Incident: what to do when the alarm fires" scheduled (`future: true`, `days_ago: -5` and `-12`); `next_date_days_ahead: 5`; series row gains `"featured": true` → Seeder writes `ttm_featured` so the hub features it deterministically; part 3 gains category `security` (second) and tags `wordpress`, `php`, `integrity`.
- Journal Sunday → `posts.json` rows may carry `"weekday": "Sunday"`; the Seeder moves the computed date back to the most recent such weekday (0–6 days), so "A rainbow over the rail yard" always renders "Sunday · Portland" whatever day the seed runs.
- Book form label → `Fiction\Books` form `collection` renders as "Stories" (the mock's "Stories · 2019 · paperback"); other labels unchanged.
- About page image → the Seeder sets a 3:2 (`ttm-thumb`) featured image on the About page; `page.html` renders `core/post-featured-image` with class `ttm-page__portrait` above `post-title` (nothing renders when a page has no thumbnail).
- Writing ≤ 1024 order → `.ttm-writing-body > main, .ttm-writing-body > aside { display: contents }` at ≤ 1024 with `order` on the four section groups (All serials 1, Short fiction 2, Recent chapters 3, In print 4). `display: contents` on landmarks is exposed by current Chromium; the `a11y` row treats `landmark-*`/`region` findings as moderate, not blocking.
- 404 "Latest" numbers → CSS counters (`.ttm-latest ol` counter-reset; `.ttm-latest .ttm-numbered__row::before` `counter(n, decimal-leading-zero)`) on a `core/query` post-template; no plugin block.
- Screenshots → `scripts/screenshots.mjs` `ZONES` becomes exactly the 14 files of §6.11 (each with a `path`), `OUT_DIR` `docs/feedback/phase-3/`; the phase-2 PNGs stay committed untouched.
- e2e screen set → `tests/e2e/lib/urls.mjs` `SCREENS` = `front '/'`, `article '/signing-your-options-table/'`, `journalPost '/journal-post-1/'`, `writing '/writing/'`, `securityArchive '/category/security/'`, `journalArchive '/category/journal/'`, `seriesHub '/series/'`, `seriesHardening '/series/hardening-wordpress/'`, `seriesEntry '/series/the-quiet-ledger/'`, `tagArchive '/tag/wordpress/'`, `search '/?s=ledger'`, `about '/about/'`, `notFound '/this-page-does-not-exist/'`; `SCREEN_URLS` follows. `securityFiltered '/category/security/?tag=wordpress'` is exported separately (fidelity and selectors only).
- Fidelity mechanics → as phase 2 (`tests/e2e/lib/{presets,style}.mjs`, project `fidelity`, colours from `theme.json`, grids as resolved px tracks, `margin-left: auto` asserted by position). New: text rows use `innerText()` normalised with `.replace(/\s+/g, ' ').trim()`; "visible" rows use `isVisible()`; count rows count DOM nodes; `≈` rows use `toBeCloseTo(value, 0)`; tolerance ±0.5px means `Math.abs(a − b) ≤ 0.5`.
- Reading-time targets in the seed are met by adjusting each row's `paragraphs` count against `prose.json` and recording the measured `ttm_word_count` in the commit body; no lorem, no generated filler.

## Conventions
- Commit message: title `<ID>: <title>`; body `Goal:` / `Tests:` / `Interpretation:` / `Measurement:` (tuning only) / `Manual check:` — template in `CLAUDE.md`.
- After every task: `docs/foundry.json` `verify` (composer lint, composer test:unit, npm run lint, npm run test:unit, npm run build, forbidden-patterns) plus `extraVerify` for the touched prefixes. `npm run test:e2e` and `npm run test:integration` need Docker; e2e starts wp-env and reseeds itself. If wp-env reports the theme broken after a branch switch: `npx wp-env stop && npx wp-env start`.
- "Un-fixme row X" means: in `tests/e2e/fidelity.spec.mjs` change that row's `test.fixme(` to `test(`, delete its `// P<n>-<nn>` tag, and make it pass; never delete a row. Row ids are §6.9's `id` column; test names are `'<id>: <selector> @<viewport>'`.
- Fidelity overrides (Decision "Colour vs a11y"): `nav-current`/`nav-hub-current` → accent-700; `js-words`, `wr-chapter-num`, `ar-mostread-num`, `hub-part-num` → neutral-700; `wr-serial-status` "Complete" → neutral-700; `box-btn` background → accent-700. Everything else §6.9 verbatim.
- Every CSS addition goes under its `01 §4` numbered comment header in `ttm.css` (`/* 4.18 series bar */`, `/* 4.22 article head + byline */`, …; add `/* 0.2 page container */`, `/* 4.11 journal stream row */`, `/* 4.12 journal post header */`, `/* 4.13 syndication line */`, `/* 4.23 figure */`, `/* 4.25 archive head */`, `/* 4.26 filter row */`, `/* 4.27 year group */`, `/* 4.28 pagination */`, `/* 4.29 numbered rows */`, `/* 4.35 story tile */`, `/* 4.36 book cover */`, `/* 4.37 stat row */`, `/* 4.38 series featured */` where missing). No hex literals; presets via `var(--wp--preset--…)`; rules via `var(--ttm-rule-1|2)`.
- Every `ttm-*` class a task adds or renames is updated in the markup, `ttm.css`, `scripts/css-coverage-allow.txt` (remove its pending line), `tests/e2e/selectors-allow.txt` (remove its pending line), the fidelity rows and the PHP tests in the same commit (rule 46).
- Group blocks whose layout `ttm.css` defines use `"layout":{"type":"default"}` literally; no `layout: constrained` anywhere inside an `is-style-grid-*` group (rule 36 extended). Column children of grids are `layout: default` groups.
- Config keys are read as `Config::get( 'key', <default> )` with the default equal to `Config::defaults()` (`tests/unit/ConfigFallbacksTest.php`).
- Seed fixture strings are original prose or the mock's exact copy from `docs/Eric Mann Newspaper.dc.html` (line ranges in SPEC's preamble); `grep -ri lorem docs/fixtures/seed/` stays empty (rule 39).
- The `## Log` entry for a task lists rows un-fixme'd, pending lines removed, interpretation choices, measurements, and `Manual check:` (or `none`).
- Row-to-task map (for the reviewer): container-* P0-09 · nav-*, mast-inner-* P0-10 · footer-inner-rule P0-11 · bar-* P1-01 · art-row, art-row-layout, art-hero, art-hero-phone, art-body, art-h2, art-pre, art-pull, art-link, aside-sticky P1-02 · art-kicker, art-h1, art-h1-phone, art-dek, art-dek-code, art-byline, art-byline-author, art-byline-tags P1-03 · prevnext-* P1-04 · toc-* P1-05 · more-*, box-*, aside-phone-order P1-06 · jr-* P2-01 · js-* P2-02 · aj-* P2-03 · ar-head, ar-kicker, ar-h1, ar-h1-phone, ar-desc, ar-stats*, tag-kicker P3-01 · ar-filter-* P3-02 · ar-body, ar-year*, ar-row*, ar-pagination*, ar-phone-year, tag-aside P3-03 · ar-aside-*, ar-mostread-*, ar-tablet-aside P3-04 · search-*, 404-*, page-* P3-05 · hub-head, hub-h1, hub-desc, hub-stats, hub-featured, hub-kicker, hub-title, hub-dek, hub-progress*, hub-meta, hub-buttons, hub-parts, hub-part* P4-01 · hub-all-head, hub-grid*, hub-nobox, hub-phone P4-02 · single-* P4-03 · wr-hero, wr-cover, wr-kicker, wr-title, wr-synopsis, wr-buttons, wr-stats, wr-stat-* P4-04 · wr-body, wr-serials-head, wr-serial-*, wr-chapters-head, wr-chapter-* P4-05 · wr-tiles*, wr-tile*, wr-books, wr-book-*, wr-phone P4-06 · a11y, network, selectors, seed-hero-color P5-02.

## Phase 0 — Harness and shared chrome

### P0-01: Allow-list to pending lines, budget 61440, tagged fixme guard
**Goal:** Turn the four phase-2 glob lines into one `# P<n>-<nn> pending` line per unstyled class, make both lint guards accept only tagged entries during the flight, and raise `cssBudgetBytes` to 61440.
**Files touched:** `scripts/css-coverage-allow.txt`, `scripts/check-css-coverage.mjs`, `scripts/lib/css-coverage.mjs`, `scripts/test/check-css-coverage.test.js`, `scripts/check-fixme.mjs`, `scripts/check-budget.mjs`, `CLAUDE.md` (budget line only), `docs/SETUP.md` (one paragraph).
**Design constraints:** SPEC §3.1 rule 30 (61440 ⚠️ ASSUMPTION lives only in `scripts/check-budget.mjs` and `CLAUDE.md`), rule 34 amendment (allow-list empty at flight end; single-class pending lines only), §6.9 last paragraph (fixme rows exist during the flight), Decision "Harness during the flight". Method: delete the four glob lines; run `node scripts/check-css-coverage.mjs`; for every class it reports (SPEC counts 32) add `<class> # P<n>-<nn> pending` using the row-to-task map in Conventions (series-bar/toc/entry → P1-xx; syndication/journal-stream → P2-xx; archive/filter-row/most-read/category-stats → P3-xx; series-featured/progress/stats/single/hub-head → P4-01..03; serial-hero/book → P4-04..06). `parseAllowList()` gains `strict` validation: a line not matching `/^ttm-[a-z0-9_-]+ # P\d-\d\d pending$/` is an error; `report()` returns `pendingCount`; the CLI prints `css-coverage: N pending` and, when the exported constant `ALLOW_PENDING` is `false`, fails on any line (the < 10 cap is removed). `check-fixme.mjs`: a `test.fixme(` line is tolerated only when it contains `// P\d-\d\d` and `ALLOW_TAGGED` is `true`; print the tagged count; untagged occurrences still fail. Update the SETUP paragraph on the fidelity suite to describe tagged fixme rows.
**Acceptance tests:** `scripts/test/check-css-coverage.test.js`: `parseAllowList rejects a glob line in strict mode`, `parseAllowList accepts a single-class pending line`, `report counts pending lines`; new `scripts/test/check-fixme.test.js` (extract the line filter into `scripts/lib/fixme.mjs` `taggedFixmeHits(lines, allowTagged)`): `untagged fixme fails`, `tagged fixme passes while allowed`, `tagged fixme fails when not allowed`.
**Out of scope:** Styling any class; touching `ttm.css`; adding fidelity rows.
**Verification:** `npm run lint` (coverage prints `32 pending` or the real count, 0 missing, 0 dead); `npm run test:unit`; `git diff --stat` shows no `docs/phase-*/` change.
**Depends on:** none

### P0-02: Config keys, `article-h2` slug and the theme.json variable check
**Goal:** Add `series.related_limit` and `seed.image_band_angle`, rename the `h2` font-size slug to `article-h2`, and make `scripts/check-theme-json.mjs` fail on any preset variable reference no slug generates (rule 44).
**Files touched:** `plugins/ttm-core/src/Config.php`, `tests/unit/ConfigFallbacksTest.php` (only if the scanner needs the new keys), `tests/unit/ConfigTest.php`, `themes/ttm-theme/theme.json`, `themes/ttm-theme/assets/css/ttm.css` (the three `--font-size--h2`/`--h-2` references only), `scripts/check-theme-json.mjs`, `scripts/lib/theme-json-vars.mjs` (new), `scripts/test/theme-json-vars.test.js` (new).
**Design constraints:** SPEC §5 (keys and defaults: `series.related_limit = 4`, `seed.image_band_angle = 30` ⚠️ ASSUMPTION), §3.2 rule 44, Decision "Rule 44 slug" (slug `article-h2` generates `--wp--preset--font-size--article-h-2`; the checker kebab-cases with `/(?<=[a-z])(?=\d)/` → `-` and `/(?<=\d)(?=[a-z])/` → `-`, lower-cased), rule 24 (defaults only in `Config.php`). `theme.json`: rename slug `h2` → `article-h2` (name "Article heading 2"); `styles.elements.h2.typography.fontSize` → `var(--wp--preset--font-size--article-h-2)`. `ttm.css`: `.is-style-journal-title` and `.entry-content h2` → `--article-h-2`; `.ttm-lead__title` phone rule → `--article-h-2` (drop the "h-2" comment). `theme-json-vars.mjs` exports `generatedVars(themeJson)` (set of `--wp--preset--font-size--<kebab>` and `--wp--preset--color--<kebab>`) and `referencedVars(cssText)` (every `--wp--preset--(font-size|color)--[a-z0-9-]+`); the checker runs both over `ttm.css`, `style.css` and `JSON.stringify(theme.styles)` and fails listing unknown references; `expectedFontSizes` gains `'article-h2': '30px'` and drops `h2`.
**Acceptance tests:** `tests/unit/ConfigTest.php`: `test_series_related_limit_default_is_four`, `test_seed_image_band_angle_default_is_thirty`; `scripts/test/theme-json-vars.test.js`: `generatedVars kebab-cases letter digit boundaries` (`article-h2` → `article-h-2`, `h1` → `h-1`... note `h1` generates `--h-1` too: the existing `--font-size--h1` references in ttm.css must be checked; if the test reveals `h1` is referenced as `--h1`, fix those references to `--h-1` in the same commit and record it), `referencedVars finds font-size and color references`, `unknown reference is reported`.
**Out of scope:** Any other CSS change; the Seeder (P0-05).
**Verification:** `npm run lint` (`check:theme-json` prints ok); `composer test:unit`; `grep -rn 'font-size--h2\b\|font-size--h-2\b' themes/` returns nothing.
**Depends on:** P0-01

### P0-03: Runtime selector coverage spec (rule 41) and the e2e screen set
**Goal:** Add `tests/e2e/selectors.spec.mjs` with its pure CSS-selector parser and allow-list, and extend the e2e screen set to SPEC §1 "Done".
**Files touched:** `scripts/lib/css-selectors.mjs` (new), `scripts/test/css-selectors.test.js` (new), `tests/e2e/selectors.spec.mjs` (new), `tests/e2e/selectors-allow.txt` (new), `tests/e2e/lib/urls.mjs`, `tests/e2e/playwright.config.mjs` (`fidelity` project `testMatch` gains `selectors`; `desktop`/`phone` `testIgnore` likewise), `tests/e2e/specs/screens.spec.mjs` (the `fetchpriority` check stays on `front` and `article` only), `docs/SETUP.md` (one paragraph).
**Design constraints:** SPEC §3.2 rule 41, §1 "Done" URLs, Decisions "Rule 41 selectors spec" and "e2e screen set". `css-selectors.mjs`: `selectorsOf(cssText)` strips comments, walks `{`/`}` nesting tracking `@media` depth, splits prelude on commas outside parentheses, returns `[{selector, inMedia}]`; `queryable(selector)` strips `::before|::after|::-webkit-*`; `isExempt(entry)` implements the auto-exemptions in the Decision; `parseSelectorAllow(text)` mirrors the coverage allow-list format. The spec builds the screen list from `urls.mjs` (`SCREEN_URLS` + `securityFiltered`), visits each at 1280×900, evaluates every queryable selector once per page, and asserts the zero-match set minus the allow-list is empty (one `test` per screen set, failing with the full list). Seed the allow-list by running the spec: every selector reported today gets `# P<n>-<nn> pending` (map by component as in P0-01) or `# editor block style (04 §3)` for `.is-style-*` styles no template uses (`is-style-tile`, `is-style-zone`, `is-style-cover`, `is-style-cover-shadow`, `is-style-numbered-rows`, `is-style-short`, `is-style-headline-s`, `is-style-display-l` on paragraphs, `is-style-lead`, `is-style-poster` on paragraphs, button styles and similar). `urls.mjs` per the Decision (article becomes `/signing-your-options-table/`; `seriesEntry` stays).
**Acceptance tests:** `scripts/test/css-selectors.test.js`: `selectorsOf splits comma lists and flags media rules`, `selectorsOf ignores comments containing braces`, `queryable strips pseudo-elements`, `isExempt covers hover focus state classes and media`; `npm run test:e2e` runs `selectors.spec.mjs` green with the seeded allow-list; `screens.spec.mjs` green on all 13 screens (fix any axe serious/critical the new screens surface only if it is a one-line markup fix; otherwise allow-list nothing and record the finding for the owning phase task).
**Out of scope:** Fixing any selector; CSS changes.
**Verification:** `npm run lint`; `npm run test:unit`; `npm run test:e2e`; `wc -l tests/e2e/selectors-allow.txt` recorded in the log.
**Depends on:** P0-01

### P0-04: Fidelity rows for every §6.9 row as tagged fixme
**Goal:** Transcribe every row of SPEC §6.9 into `tests/e2e/fidelity.spec.mjs` as `test.fixme(` tagged with its owning task, grouped by screen, without touching the phase 2 rows.
**Files touched:** `tests/e2e/fidelity.spec.mjs`, `tests/e2e/lib/style.mjs` (add `text(locator)` = normalised innerText, `visibleCount(locator)`), `tests/e2e/lib/urls.mjs` (only if a helper path is missing).
**Design constraints:** SPEC §6.9 (transcribe in table order; a row with several properties is one test with several `expect`s; the `1920` container rows set viewport 1920×900; `ar-tablet-aside` sets 1000×900), rule 38, Decisions "Fidelity mechanics" and "Colour vs a11y" (write the overridden expectation, not SPEC's literal, for the listed rows), Conventions "Row-to-task map" (each `test.fixme(` line ends with `// P<n>-<nn>`). Describe blocks: `container`, `nav`, `masthead inner`, `footer inner`, `series bar`, `article`, `prev/next`, `aside`, `journal`, `journal stream`, `writing`, `archive`, `journal archive`, `tag archive`, `search`, `404`, `page`, `hub`, `single series`, `seed`, plus the existing `accessibility`/`network` describes gain a per-screen loop over `SCREEN_URLS` at 1280 and 390 as `test.fixme` tagged P5-02 (keep the front-page tests as they are). A `gotoScreen(page, path, width)` helper replaces `gotoFront` for the new rows (`gotoFront` stays). Each test's body is a real assertion so un-fixme-ing is a one-token change. The `seed-hero-color` row samples the centre pixel by drawing the `<img>` to a canvas in `page.evaluate` and asserts the Euclidean RGB distance from `(210,48,19)` is ≥ 40. The `selectors` row is satisfied by `selectors.spec.mjs` (P0-03) and is not duplicated here.
**Acceptance tests:** `npm run lint` (`check:fixme` prints the tagged count, 0 untagged); `npx playwright test --config tests/e2e/playwright.config.mjs --project fidelity --list` lists one entry per §6.9 row (count them; record the number in the log); `npm run test:e2e` green (phase 2 rows pass, new rows skipped).
**Out of scope:** Making any new row pass; CSS or PHP.
**Verification:** as above.
**Depends on:** P0-03

### P0-05: Seed images per rule 45, author display name, book and About covers
**Goal:** Replace the solid-red seed PNGs with neutral photograph placeholders and 2:3 titled covers, give the admin user the display name "Eric Mann", and seed covers for books and a 3:2 image for the About page.
**Files touched:** `plugins/ttm-core/src/Cli/Seeder.php`, `docs/fixtures/seed/books.json` (`"cover": true` rows), `docs/fixtures/seed/pages.json` (`"featured_image": true` on `about`), `tests/integration/Cli/SeederTest.php`, `tests/integration/Blocks/BookGridTest.php` (only if the seeded shape changed a fixture it uses).
**Design constraints:** SPEC §3.2 rule 45 (no red anywhere: `grep -n '210, 48, 19' plugins/ttm-core/src/Cli/Seeder.php` returns nothing), §6.10 last bullet, §5 `seed.image_band_angle` ⚠️ ASSUMPTION read only as `Config::get( 'seed.image_band_angle', 30 )`, rule 9 (no clock), rule 24 (colour components live in `private const` arrays on the Seeder, e.g. `FIELD = [ 186, 182, 182 ]` neutral-400, `BAND = [ 155, 151, 151 ]` neutral-500, `BORDER = [ 215, 211, 211 ]` neutral-300, `COVER = [ 96, 93, 93 ]` neutral-700, `COVER_TEXT = [ 248, 244, 244 ]` neutral-100 — values from `docs/_ds/…/styles.css`). `image( $label, $size_key )`: fill FIELD; draw a filled polygon band (width ≈ 22 % of the shorter side) rotated by the config angle whose position along the diagonal is `crc32( $label ) % width` (deterministic per label); 2px BORDER rectangle inset at the edge. New `cover( $title )`: `ttm-cover` size, COVER fill, title centred with `imagestring( 5 )` wrapped at 18 characters, 2px BORDER. `seed_series()` and `seed_books()` (`cover: true` rows) use `cover()`; `seed_pages()` sets a `ttm-thumb` `image()` thumbnail on rows with `featured_image`; `seed_posts()` unchanged. Add `wp_update_user( [ 'ID' => 1, 'display_name' => 'Eric Mann' ] )` in `run()` (author of every seeded post; the string is a translatable literal like the tagline).
**Acceptance tests:** `SeederTest`: `test_generated_image_is_neutral_with_no_red_pixels` (sample 20 pixels of the generated PNG with `imagecolorat`; every R−G ≤ 20 and R < 200), `test_cover_is_two_by_three_and_darker_than_field`, `test_books_with_cover_flag_get_cover_ids`, `test_about_page_has_three_by_two_thumbnail`, `test_admin_display_name_is_eric_mann`; existing `test_generated_image_is_an_attachment_with_alt` stays green.
**Out of scope:** Seed copy (P0-06..08); the fidelity `seed-hero-color` row (P5-02).
**Verification:** `npm run test:integration`; `npx wp-env run cli wp ttm seed --reset` then open `http://localhost:8888/signing-your-options-table/` — hero is grey with a band, not red. `Measurement:` in the commit body: centre-pixel RGB of the seeded article hero before (210,48,19) and after.
**Depends on:** P0-02

### P0-06: Tuning — `seed.image_band_angle`
**Goal:** Measure the placeholder band at three angles and settle the default once.
**Files touched:** `plugins/ttm-core/src/Config.php` (only if the number changes), `docs/PROGRESS.md`.
**Design constraints:** SPEC §5 (`seed.image_band_angle` ⚠️ ASSUMPTION), Decision S7. Method: with the seeded site running, override the key through an mu-plugin `ttm_config` filter (a temporary file under `wp-content/mu-plugins/` inside the container, removed afterwards; never committed) at 15, 30 and 45; reseed each time; record for each angle whether the band stays visible inside the `ttm-thumb` 3:2 crop and the `ttm-cover` 2:3 crop (`wp media image-size` crops) by sampling the crop's centre row for the BAND colour. Keep 30 unless a crop loses the band; record the outcome in the commit body `Measurement:` and the log.
**Acceptance tests:** none new; `composer test:unit` green.
**Out of scope:** Any other Seeder change.
**Verification:** `npm run test:integration` (unchanged); `git status` clean of mu-plugin files.
**Depends on:** P0-05

### P0-07: Seed — the `2b` article and the Hardening WordPress series
**Goal:** Give "Stop trusting the database: signing your options table" the mock's body, caption, tags and reading time, and complete the Hardening WordPress series with parts 4–6.
**Files touched:** `docs/fixtures/seed/posts.json`, `docs/fixtures/seed/series.json`, `plugins/ttm-core/src/Cli/Seeder.php` (`caption` row field → attachment `post_excerpt`; `featured` series field → `ttm_featured`; `weekday` field per Decision "Journal Sunday" may land here or in P0-08), `tests/integration/Cli/SeederTest.php`, `tests/e2e/lib/urls.mjs` (no change expected; confirm the article slug).
**Design constraints:** SPEC §6.10 first bullet, Decision "Hardening WordPress seed" (restated: part 3 gains second category `security` and tags `wordpress`, `php`, `integrity`; part 4 slug `hardening-part-4-keys-in-the-environment`, title "Hardening WordPress, part 4: keys in the environment, rotated live", `part_title` "Keys in the environment, rotated live", category `security`, `days_ago: 0`, published; part 5 slug `hardening-part-5-the-admin-with-the-weak-password`, `part_title` "The admin with the weak password", `future: true`, `days_ago: -5`; part 6 slug `hardening-part-6-incident-when-the-alarm-fires`, `part_title` "Incident: what to do when the alarm fires", `future: true`, `days_ago: -12`; series `next_date_days_ahead: 5`, `"featured": true`; every published part gets a one-sentence `excerpt`), rule 39. Article `content` (block markup prefix, verbatim from mock `2b` lines 78–95): paragraph, paragraph with three inline `<code>`, `core/heading` h2 "What a signature buys you", paragraph, `core/code` with the 12-line `add_action` snippet, paragraph with inline `<code>options_tampered</code>` and one inline link to `/hardening-part-2-salts-and-keys/` (the `art-link` row needs a link in a body paragraph), `core/quote` `{"className":"is-style-pull"}` "A file-integrity scanner watches the doors. The options table is a window that was never on the blueprint." (curly quotes, no cite), `core/heading` h2 "Where to keep the key", paragraph; then `paragraphs: N` prose so `ttm_word_count` lands in 2991–3220 (14 min at 230 wpm). `excerpt` = the mock's dek with `<code>wp_options</code>`. `caption`: "Caption in the theme's meta type. Photographs are grayscale only on the front page and archives." `alt` stays. Seeder: `caption` → `wp_update_post( [ 'ID' => $attachment_id, 'post_excerpt' => … ] )`; `featured` → `update_term_meta( $term_id, 'ttm_featured', 1 )` (delete when absent so reseeds are idempotent).
**Acceptance tests:** `SeederTest`: `test_seeded_article_reads_fourteen_minutes` (word count within 2991–3220), `test_seeded_article_has_two_h2_a_code_block_and_a_pull_quote`, `test_hardening_series_has_four_published_and_two_scheduled_parts`, `test_hardening_is_the_featured_series`, `test_seeded_article_hero_has_caption`; existing `test_seeded_lead_is_signing_your_options_table` and `FrontPageTest::test_series_strip_lists_the_three_nonfiction_series_newest_first` stay green.
**Out of scope:** Journal/Writing/Security seed (P0-08); any theme change.
**Verification:** `npm run test:integration`; `npm run test:e2e` (phase 2 rows still green — the lead must still be part 3); `grep -ri lorem docs/fixtures/seed/` empty. `Measurement:` the measured word count and `paragraphs` value.
**Depends on:** P0-05

### P0-08: Seed — journal, Writing, Security archive and series hub copy
**Goal:** Carry the mocks' copy for the journal post, the Writing page, the Security archive and the series hub into the fixtures.
**Files touched:** `docs/fixtures/seed/posts.json`, `docs/fixtures/seed/series.json`, `docs/fixtures/seed/books.json`, `docs/fixtures/seed/categories.json`, `plugins/ttm-core/src/Cli/Seeder.php` (`weekday` row field), `tests/integration/Cli/SeederTest.php`, `tests/integration/Theme/FrontPageTest.php` and `tests/integration/Theme/HubWritingTemplatesTest.php` (only where a title/count they assert changes).
**Design constraints:** SPEC §6.10 bullets 2–5, rule 39, Decisions "Journal Sunday", "Book form label", "Reading-time targets". Journal: `journal-post-1` gets `content` = the four mock paragraphs (2c lines 150–153, no prose), `"weekday": "Sunday"`, `location` "Portland", `syndication` `{x, mastodon}` URLs; `journal-post-2` "Two working weeks a year" and `journal-post-3` "Rain, Psalm 46, a quiet build server" get original `content` of ≈ 184 and ≈ 96 words (no prose); at least nine Journal posts remain. Writing: `the-quiet-ledger` `description` = mock synopsis (2d line 193, "A forensic accountant in a company town…"), `genre` "literary thriller", `cadence` "monthly", `next_date_days_ahead` so the hero reads a real date, `cover: true`; chapters 1–12 get `part_title`s (ch. 12 "Reconciliation" already) and one-line `excerpt`s; raise the twelve published chapters' `paragraphs` until `Serials::stats()['avg_minutes']` is between 10 and 16; `failover` description "Three engineers, one data center, and the night the generators did not start."; `salt-water-wires` description "A cable-landing station on the Oregon coast, and the family that has kept it running for sixty years."; stories: "The Last Cron Job" (`paragraphs` for ≈ 3,100 words, year current), "A Field Guide to Empty Offices" (≈ 1,800 words, `days_ago` ≥ 400 so the year is last year), "Uptime" (keeps its cover), new `story-what-the-river-audits` "What the River Audits" (no image); `books.json` = "Salt Water Wires" (`novel`, 2022, formats paperback, ebook, one link, cover) and "Eleven Small Doors" (`collection`, 2019, paperback, cover). Security: category description "Application security for people who ship. Threat models, disclosures, and the occasional post-mortem — written from the engineer's side of the incident."; add original Security posts (titles, one-sentence excerpts, `paragraphs` 8–14) so the category has ≥ 14 published posts spanning ≥ 2 years with page 1 (12 rows) containing two year groups; tags distributed so the top five within Security are `wordpress`, `threat-modeling`, `cryptography`, `disclosure`, `passwords` and `?tag=wordpress` returns ≥ 3 rows; `most_read: true` on "Nonces are not CSRF tokens", "Reading a CVE like an engineer" and new "Why I still recommend hardware keys to my parents" (and off every other Security post). Series hub: `hardening-wordpress` description "Six parts on defending the parts of a WordPress install that scanners ignore: configuration, keys, the database, and the people with logins."; keep six series across four categories. Seeder `weekday`: after computing `$date`, if the row has `weekday`, subtract days until `format('l')` matches (0–6 days).
**Acceptance tests:** `SeederTest`: `test_journal_post_one_is_on_a_sunday_with_location_and_syndication`, `test_journal_word_counts_near_the_mock` (post 2 in 160–210, post 3 in 80–115), `test_quiet_ledger_has_synopsis_genre_cadence_and_titled_chapters`, `test_security_has_two_year_groups_on_page_one_and_a_second_page`, `test_security_top_tags_are_the_mock_five`, `test_three_security_posts_are_most_read`, `test_books_are_salt_water_wires_and_eleven_small_doors`; existing `test_seeded_journal_excerpts_are_38_to_48_words`, `test_seeded_quiet_ledger_latest_chapter_is_reconciliation`, `test_seeded_writing_essays_derive_as_story_but_stay_older_than_the_last_cron_job` stay green (adjust `days_ago` of the two Writing essays if the new story reorders "Also running"; the front-page order must still end with The Last Cron Job).
**Out of scope:** Theme or block changes; images (P0-05).
**Verification:** `npm run test:integration`; `npm run test:e2e` (phase 2 rows green); `grep -ri lorem docs/fixtures/seed/` empty. `Measurement:` word counts reached and the chapter `paragraphs` value.
**Depends on:** P0-07

### P0-09: Page container (rule 42) and the `container-*` rows
**Goal:** Bound every page to a centred 1280px column with the gutter as inline padding, without moving a front-page row.
**Files touched:** `themes/ttm-theme/assets/css/ttm.css` (`/* 0.2 page container */`), `tests/e2e/fidelity.spec.mjs` (rows `container-width`, `container-gutter`, `container-phone`, `container-front`), `tests/integration/Theme/TemplatesShellTest.php`.
**Design constraints:** SPEC §3.2 rule 42, §6.1.0, Decision S1 (keep `theme.json` root padding; add `.wp-site-blocks { max-width: 1280px; margin-inline: auto; padding-inline: var(--wp--custom--gutter--desktop); box-sizing: border-box; }`), §2 non-goal (no phase 2 row moves: run the full `fidelity` project before and after), rule 30 (budget), rule 37. Full-bleed elements keep breaking out via core's root-padding negative margins (the poster) — verify `poster` row still passes at 1280 and the poster is 1280 wide at 1920.
**Acceptance tests:** un-fixme `container-width` (1920: width 1280px, margin-left 320px), `container-gutter` (1280: padding-left 48px, `clientWidth − paddings` = 1184), `container-phone` (390: padding-left 20px), `container-front` (1920: `.ttm-lead-row` width 1184px); `TemplatesShellTest::test_site_blocks_wrapper_is_present_on_every_template` (renders `page`, `404`, `index` and asserts the `wp-site-blocks` class is emitted).
**Out of scope:** Masthead/nav (P0-10); any inner component.
**Verification:** `npm run lint`; `npm run test:e2e` (every phase 2 row green plus the four container rows); at 1920 in a browser `/` and `/about/` are centred.
**Depends on:** P0-04

### P0-10: Inner masthead and phone-only overlay nav (rule 43, §6.1.1)
**Goal:** Render the inner masthead as the mock's one-row grid with the nav inline at ≥ 721px and the "Menu" overlay at ≤ 720px, with no overlay chrome visible when closed.
**Files touched:** `themes/ttm-theme/patterns/masthead-inner.php`, `themes/ttm-theme/parts/header-inner.html`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.2 masthead inner */`, `/* 4.3 nav */`), `themes/ttm-theme/assets/js/nav.js` (only if the 720 breakpoint needs the `ttm-nav-open` toggle adjusted), `tests/e2e/fidelity.spec.mjs` (rows `nav-inline`, `nav-item`, `nav-nochrome`, `nav-current`, `nav-hub`, `nav-hub-current`, `nav-phone-menu`, `nav-phone-hidden`, `mast-inner`, `mast-inner-title`, `mast-inner-by`, `mast-inner-phone`), `tests/integration/Theme/ChromePartsTest.php`, `tests/integration/Nav/CurrentSectionTest.php`, `plugins/ttm-core/src/Nav/CurrentSection.php`.
**Design constraints:** SPEC §6.1.1, §3.2 rule 43, rule 36 extended (header part group `layout: default`), Decisions S2, "Inner masthead", "Nav current section" (remove the `is_singular && SeriesIndex::for_post → '/series/' current` branch; update `test_body_classes_on_single_series_post` / add `test_single_post_in_series_does_not_mark_series_current`), "Colour vs a11y" (`nav-current`/`nav-hub-current` accent-700), rule 6 (no new front-end JS beyond `nav.js`). Pattern: `div.ttm-masthead-inner` (`layout: default`) > `div.ttm-masthead-inner__title` (`layout: default`; `core/site-title {"level":0}` + `p.ttm-masthead-inner__by`), `core/navigation {"className":"ttm-nav ttm-masthead-inner__nav","overlayMenu":"mobile","hasIcon":false,"ariaLabel":"Sections"}` with the same link list (hub link keeps `ttm-nav__hub`), `p.ttm-masthead-inner__aside` Newsletter link. CSS: grid `auto 1fr auto`, align center, gap 32px, padding 14px 0 12px, `border-bottom: var(--ttm-rule-2)` (remove the current `border-top`); title 22px/800/−0.02em; `__by` 12px neutral-700 margin-left 10px, baseline; nav `ul` flex gap 22px, 13px/600; `.ttm-masthead-inner__nav .ttm-nav__hub > a` 400 neutral-700 (no `margin-left: auto` on the inner nav); `.current-menu-item > a` accent-700 (also for the hub item); aside 12px neutral-700. `@media (min-width: 721px)`: `.ttm-masthead-inner .wp-block-navigation__responsive-container-open, .ttm-masthead-inner .wp-block-navigation__responsive-container-close { display: none }` and the responsive container static/inline (`display: block; position: static; background: none`). `@media (max-width: 720px)`: grid `1fr auto`, padding 12px 0 10px, title 18px, `__by` and `__aside` hidden, the open button visible as text "Menu" 11px neutral-700 (no border, no background, padding 0), `.wp-block-navigation__responsive-container:not(.is-menu-open) { display: none }` (covers 600–720 where core would show it), open overlay: ground background, items 24px/800 stacked with 1px rules, close button 24px top-right (text "Close"). Keep the existing `.ttm-nav .current-menu-item > a` rule shared with the front masthead.
**Acceptance tests:** un-fixme the twelve rows listed; `ChromePartsTest::test_header_inner_renders_menu_toggle_text` updated for `overlayMenu: mobile` (open button text "Menu", no icon) and new `test_inner_masthead_title_is_a_paragraph_with_by_line`; `CurrentSectionTest` per the Decision.
**Out of scope:** Footer (P0-11); the series bar under the masthead (P1-01).
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e` (phase 2 rows `mast-*` untouched and green); open `/signing-your-options-table/` at 1280 (nav inline, no "Close") and 390 (only title + "Menu"; tapping Menu opens the overlay; a link click closes it).
**Depends on:** P0-09

### P0-11: Inner footer variant (§6.1.2)
**Goal:** Give inner pages the 2px-ruled footer with 14px 48px padding while the front page keeps its after-poster footer, both aligned to the page gutter.
**Files touched:** `themes/ttm-theme/assets/css/ttm.css` (`/* 4.34 footer */`), `tests/e2e/fidelity.spec.mjs` (row `footer-inner-rule`; the phase 2 `footer` row must stay green as written: `16px 48px`, border 0 on `/`), `tests/integration/Theme/ChromePartsTest.php`.
**Design constraints:** SPEC §6.1.2, §6.1.0 (full-bleed elements break out with negative margins equal to the gutter), 01 §4.34. CSS: `.ttm-footer { margin-inline: calc(-1 * var(--wp--custom--gutter--desktop)); padding: 14px var(--wp--custom--gutter--desktop); border-top: var(--ttm-rule-2); }`; `.is-after-poster .ttm-footer { padding: 16px var(--wp--custom--gutter--desktop); border-top: 0; }`; phone stays 11px stacked (the gutter token is 20px there).
**Acceptance tests:** un-fixme `footer-inner-rule` (article: border-top-width 2px, padding `14px 48px`); phase 2 rows `footer`, `footer-left`, `footer-copy`, `footer-nav`, `footer-nav-sep`, `footer-phone` green; `ChromePartsTest::test_footer_after_poster_variant_has_class` green.
**Out of scope:** Footer copy or nav items.
**Verification:** `npm run lint`; `npm run test:e2e`; at 1280 the footer text on `/about/` starts at x = 48 like the masthead title.
**Depends on:** P0-10

### P0-12: Phase 0 push — screenshot script extension and baseline PNGs
**Goal:** Extend `npm run screenshots` to the 14 phase-3 files, commit the baseline set with its README, push, and record the manual check.
**Files touched:** `scripts/screenshots.mjs`, `scripts/test/screenshots.test.js`, `docs/feedback/phase-3/README.md` (new), `docs/feedback/phase-3/*.png` (14 new), `docs/feedback/README.md` (one row: `phase-3/` this flight), `docs/PROGRESS.md`.
**Design constraints:** SPEC §6.11, §8 Phase 0 "Visible result", Decision "Screenshots". `ZONES` = `[ {file:'article.png', path:'/signing-your-options-table/', viewport 1280×900, fullPage}, journal.png '/journal-post-1/', writing.png '/writing/', archive-security.png '/category/security/', series-hub.png '/series/', series-single.png '/series/hardening-wordpress/', search.png '/?s=ledger', 404.png '/this-page-does-not-exist/' (all 1280 fullPage); article-390.png, journal-390.png, writing-390.png, archive-390.png (390×844 fullPage; archive = '/category/security/'); front-1920.png '/', article-1920.png (1920×900 fullPage) ]`; `OUT_DIR = docs/feedback/phase-3`; `page.goto(BASE_URL + zone.path)`; keep the image-wait logic. README pairs each file with its mock (`design_article.png`/2b, `design_journal.png`/2c, `design_serial.png`/2d, mock `1e`, mock `1f`, mock `3b` for the 390 article, `02 §H` for search/404).
**Acceptance tests:** `scripts/test/screenshots.test.js`: `ZONES lists the fourteen phase-3 files with paths` (names and viewports), existing `unionClip`/`pendingImages` tests green.
**Out of scope:** Any CSS/PHP change; removing phase-2 PNGs.
**Verification:** `npx wp-env start && npx wp-env run cli wp ttm seed --reset && npm run screenshots` writes 14 files; commit; `git push -u origin HEAD`. Log: `Manual check: NOT VERIFIED (human) — compare docs/feedback/phase-3/article.png's masthead against the top of docs/feedback/design_article.png; every inner page should be a centred 1280 column with an inline nav and no "Close" button`.
**Depends on:** P0-11

## Phase 1 — Article

### P1-01: Series bar (§6.2, 01 §4.18)
**Goal:** Make `ttm/series-bar` render and style as the mock's `10px 1fr auto` bar with segments and "View series", collapsing per `3b` on the phone.
**Files touched:** `plugins/ttm-core/blocks/series-bar/render.php`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.18 series bar */`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`, `tests/e2e/fidelity.spec.mjs` (rows `bar`, `bar-mark`, `bar-name`, `bar-seg`, `bar-seg-1`, `bar-view`, `bar-phone`), `tests/integration/Blocks/SeriesBarTest.php`.
**Design constraints:** SPEC §6.2 "Series bar", rule 46 (wrapper `div.ttm-series-bar[data-ttm-block]` via `Helpers::wrapper()` unchanged), rule 2 (no inline styles), rule 32, Decision "Series bar" (three grid children: `span.ttm-series-mark.is-{status}`, `span.ttm-series-bar__text` [`span.ttm-series-bar__label` "Series · " · `a.ttm-series-bar__name` · " · " · `span.ttm-series-bar__part.tnum`], `span.ttm-series-bar__right` [`span.ttm-series-bar__segments` of `span.ttm-series-bar__seg.is-done|is-current|is-todo` + `a.ttm-series-bar__view`]). CSS: grid `10px 1fr auto`, gap 14px, align center, padding 12px 0, `border-bottom: var(--ttm-rule-1)`, 13px; label neutral-700; name 600; right flex align center; segments flex gap 4px; seg 22×4 (`is-done` neutral-900, `is-current` accent, `is-todo` neutral-300); view 12px accent-700 margin-left 14px. ≤ 720: `__label`, `__view` `display: none`, seg 12×4, gap 3px, font 12px. F11/F23 behaviour unchanged.
**Acceptance tests:** un-fixme the seven `bar-*` rows; `SeriesBarTest`: existing tests updated to the new spans, plus `test_bar_has_text_and_right_groups_with_view_link`; `test_f23_open_ended_bar_text_and_trailing_todo_segment` green.
**Out of scope:** TOC, prev/next, anything else on the article.
**Verification:** `npm run lint` (pending lines for `ttm-series-bar*` removed); `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P0-12

### P1-02: Article body row, hero, body typography and sticky aside
**Goal:** Lay out `single.html` as the mock's 8/4 grid with unconstrained columns, a colour captioned hero, the §4.24 body typography on `.ttm-entry`, and a sticky aside.
**Files touched:** `themes/ttm-theme/templates/single.html`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.23 figure */`, `/* 4.24 body typography */`, `/* 3 grids */` sticky aside), `plugins/ttm-core/src/Blocks/Helpers.php` (`featured_caption()` + registration), `tests/unit/Blocks/HelpersTest.php`, `tests/integration/Theme/ArticleTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `art-row`, `art-row-layout`, `art-hero`, `art-hero-phone`, `art-body`, `art-h2`, `art-pre`, `art-pull`, `art-link`, `aside-sticky`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.2 "Body row", "Hero", "Body", ≤ 720 bullets (hero `margin: 20px -20px 0; width: calc(100% + 40px)` 4:3; body 17/1.6; H2 24; `pre` full-bleed with 20px inner padding; pull 22), §6.1.0 (full-bleed by negative margins only), rule 36 extended (`main` and `aside` groups `layout: default`; keep `is-style-grid-8-4 ttm-article` on the row), rule 44 (`--article-h-2`), Decision "Featured-image caption" (filter on `render_block_core/post-featured-image`, `figcaption.ttm-hero__caption` 12px neutral-700 margin-top 8px; template drops `aspectRatio`; CSS `.ttm-article .wp-block-post-featured-image { margin-top: 28px; aspect-ratio: 16 / 9 }`, `img { width:100%; height:100%; object-fit: cover }`, no grayscale). Row `.ttm-article`: `column-gap: 64px; row-gap: 0; padding: 40px 0 48px`. Body: `.ttm-entry` (core adds `entry-content` too) 18/1.65 `max-width: 38em` padding-top 28px; `p` margin 0 0 22px; h2 30px/1.1/−0.02em margin 40px 0 14px; inline code 16px surface padding 1px 6px; `pre` neutral-900 bg, neutral-100 text, 14/1.55 mono, padding 18px 20px, 4px accent left rule; `.is-style-pull` 28/800/1.25/−0.015em `text-indent: -0.496em` margin 36px 0 `max-width: 26ch` (already present — verify); links underline divider offset 3px. Aside: `.ttm-article aside` display flex, column, `row-gap: 28px`, `align-self: start`, `position: sticky; top: 24px` at ≥ 1024 (move the sticky rule from `.is-style-sticky-aside` to `.ttm-article aside`; keep `.is-style-sticky-aside` on the group and its selector). ≤ 1024: `.ttm-article` single column, aside static. F12 (no image → byline-to-body gap 28px) via the body padding-top.
**Acceptance tests:** un-fixme the ten rows listed; `HelpersTest::test_featured_caption_inserts_figcaption_before_closing_figure` and `::test_featured_caption_leaves_figure_without_caption_alone` (Brain\Monkey stubs `get_post_thumbnail_id`, `wp_get_attachment_caption`, `is_singular`); `ArticleTemplatesTest`: `test_article_columns_are_not_constrained` (no `is-layout-constrained` inside `.ttm-article`), `test_hero_caption_renders_from_attachment_excerpt`, existing `test_f12_no_featured_image_renders_no_figure` green.
**Out of scope:** Article header/byline (P1-03); prev/next; aside contents.
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P1-01

### P1-03: Article header — kicker, H1, dek, byline (01 §4.22)
**Goal:** Make `ttm/article-header` render the mock's kicker, 56px H1, dek with inline code, and the ruled byline row with author, date, read time and tag chips.
**Files touched:** `themes/ttm-theme/patterns/article-header.php`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.22 article head + byline */`, `/* 2.3 type */` for `.is-style-dek-l code`), `plugins/ttm-core/src/Meta/PrimaryCategory.php` (`order_terms()` + registration), `plugins/ttm-core/src/Blocks/Helpers.php` (`style_tag_terms()`), `tests/unit/Meta/PrimaryCategoryTest.php` (or the existing Meta unit test file), `tests/unit/Blocks/HelpersTest.php`, `tests/integration/Theme/ArticleTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `art-kicker`, `art-h1`, `art-h1-phone`, `art-dek`, `art-dek-code`, `art-byline`, `art-byline-author`, `art-byline-tags`).
**Design constraints:** SPEC §6.2 "Article header", ≤ 720 (kicker 11, H1 34, dek 17, byline wraps gap 6px 14px 12px), Decisions "Kicker term order" (`get_the_terms` filter: primary first, then `sections.order` index of the top-level ancestor, then name; front end only, taxonomy `category`) and "Byline tags" (`render_block_core/post-terms` filter adds `class="tag tag-neutral"` to each `<a>` and strips separator spans when `className` has `is-style-tags`), rule 36 (pattern group `layout: default`), rule 32. Pattern: `div.ttm-article-head` (`layout: default`) > `core/post-terms {"term":"category","className":"is-style-kicker","separator":" · "}` · `core/post-title {"level":1}` · `core/post-excerpt {"className":"is-style-dek-l"}` · `div.ttm-byline` (`layout: default`) > `core/post-author-name {"prefix":"By ","isLink":true}` · `core/post-date {"format":"F j, Y"}` · reading-time paragraph (`ttm/reading-time format=long`) · `core/post-terms {"term":"post_tag","className":"is-style-tags"}`. CSS: kicker 12px/600/uppercase/.08em accent-700 margin 0 0 14px (`.ttm-article-head .is-style-kicker`, links inherit colour); h1 56px/1.02/−0.025em/800 margin 0 0 18px −0.03em `max-width: 18ch`; dek 21/1.4 neutral-800 `max-width: 32em` margin 0 0 22px; dek `code` 18px surface padding 1px 6px; byline flex gap 18px align center padding 14px 0 `border-top: var(--ttm-rule-2)` `border-bottom: var(--ttm-rule-1)` 13px neutral-700 tnum; author link 600 text colour; `.is-style-tags { margin-left: auto; display: flex; gap: 6px }` and `.tag` 11px surface. No excerpt → the byline sits 18px under the H1 (h1 margin covers it; `.is-style-dek-l:empty { display: none }`).
**Acceptance tests:** un-fixme the eight rows; unit: `test_order_terms_puts_primary_first_then_nav_order`, `test_order_terms_leaves_other_taxonomies_alone`, `HelpersTest::test_style_tag_terms_adds_tag_classes_and_drops_separators`; `ArticleTemplatesTest::test_article_kicker_reads_primary_then_secondary_category` (seeded article → "Technology · Security") and `test_byline_tags_are_tag_chips`.
**Out of scope:** Hero and body (P1-02).
**Verification:** `npm run lint`; `composer test:unit`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P1-02

### P1-04: Prev/next (01 §4.21)
**Goal:** Style `ttm/series-prev-next` as the mock's two ruled cells with a vertical rule between, stacked on the phone.
**Files touched:** `plugins/ttm-core/blocks/series-prev-next/render.php` (only if a class is missing), `themes/ttm-theme/assets/css/ttm.css` (`/* 4.21 prev/next */`), `tests/e2e/fidelity.spec.mjs` (rows `prevnext`, `prevnext-prev`, `prevnext-label`, `prevnext-title`, `prevnext-phone`), `tests/integration/Blocks/SeriesPrevNextTest.php`.
**Design constraints:** SPEC §6.2 "Prev/next" (grid `1fr 1fr`, 2px rule above, margin-top 40px; `__prev` padding 20px 24px 20px 0 with `border-right: var(--ttm-rule-1)`; `__next` padding 20px 0 20px 24px, no left border; label 11px uppercase .08em neutral-700 margin-bottom 8px; title 18/800/1.25; missing side keeps the cell and rules; ≤ 720 stacked with `__prev` `border-right: 0; border-bottom: var(--ttm-rule-1)`, padding 20px 0, titles 16px), F11 labels unchanged. Keep the ≤ 1024 stack rule consistent with §6.2 (stack at ≤ 720 only; at 1024 the two cells stay side by side).
**Acceptance tests:** un-fixme the five rows; `SeriesPrevNextTest::test_missing_side_keeps_empty_cell` (exists or add) green.
**Out of scope:** Anything outside `.ttm-prevnext`.
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P1-03

### P1-05: Series TOC (01 §4.20)
**Goal:** Render "In this series" with numbered rows, current/published/scheduled states, the series name on desktop and "Hub →" on the phone.
**Files touched:** `plugins/ttm-core/blocks/series-toc/render.php`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.20 series toc */`, `/* 4.29 numbered rows */` shared rules), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`, `tests/e2e/fidelity.spec.mjs` (rows `toc-head`, `toc-item`, `toc-current`, `toc-scheduled`, `toc-phone-hub`), `tests/integration/Blocks/SeriesTocTest.php`, `tests/integration/Theme/HubWritingTemplatesTest.php` (chapters variant markup, if it asserts classes).
**Design constraints:** SPEC §6.2 "Aside → In this series", F23/F24, rule 46, Decision "`ttm/series-toc`" (restated: heading `div.ttm-cell-heading.is-rail` > `span.ttm-cell-heading__label` + `a.ttm-series-toc__series` (series name, hidden ≤ 720) + `a.ttm-series-toc__hub` ("Hub →", hidden ≥ 721); items `li.ttm-series-toc__item.is-current|is-published|is-scheduled` > `span.ttm-series-toc__num.tnum` "01" + `a|span.ttm-series-toc__title` (+ `title="Scheduled Sept 26"` on scheduled) + optional `span.ttm-series-toc__dek`; the chapters variant instead renders `ol.ttm-numbered > li.ttm-numbered__row > span.ttm-numbered__num + a.ttm-numbered__title + span.ttm-numbered__dek + span.ttm-numbered__date` with label "{Series name} — recent chapters" and `a.ttm-cell-heading__link` "All {published}" — the chapters CSS lands in P4-05; this task only makes the markup and keeps its classes pending), "Colour vs a11y" (numbers and the series name neutral-700). CSS (series variant): items grid `28px 1fr`, gap 10px, padding 10px 0, `border-bottom: var(--ttm-rule-1)`, 14px/1.35; `__num` 800 tnum; `.is-current .ttm-series-toc__title` 800 accent-700; `.is-published .ttm-series-toc__title` 400 text; `.is-scheduled` colour neutral-700 (no link); `__series` 11px neutral-700; `__hub` 11px accent-700 hidden ≥ 721; ≤ 720 `__series` hidden, the TOC zone gets `border-top: var(--ttm-rule-2)` and padding 18px 0 20px.
**Acceptance tests:** un-fixme the five `toc-*` rows; `SeriesTocTest`: existing tests updated to the new classes; add `test_heading_has_series_link_and_hub_link`, `test_chapters_variant_composes_heading_and_all_link`.
**Out of scope:** Chapters-variant CSS (P4-05); more-in and newsletter box (P1-06).
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P1-04

### P1-06: More in section, newsletter box copy, aside order on the phone
**Goal:** Render "More in Technology" as one label with three ruled rows, switch the newsletter box title by context, and order the aside zones after prev/next at ≤ 1024.
**Files touched:** `themes/ttm-theme/patterns/more-in-section.php`, `themes/ttm-theme/patterns/newsletter-box.php`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.20/4.13 more-in */` → rename header `/* 4.5 headline item */` scoped rules, `/* 4.32/4.33 newsletter box + poster */`), `plugins/ttm-core/src/Bindings/Sources.php`, `plugins/ttm-core/src/Bindings/Values.php`, `tests/unit/Bindings/ValuesTest.php`, `tests/integration/Bindings/ArticleSourcesTest.php`, `tests/integration/Theme/ArticleTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `more-head`, `more-row`, `more-count`, `box`, `box-title`, `box-form`, `box-btn`, `box-phone`, `aside-phone-order`).
**Design constraints:** SPEC §6.2 "More in {Category}", "Newsletter box", §6.3 (markup and providers unchanged — verify by reading `Newsletter\Form::render()` and `Handler`; no edit), F13, Decisions S3 (`ttm/newsletter-copy`: "Get the next part" on a single post in a series or `is_tax('series')`, else "The weekly issue."; pure `Values::newsletter_title( bool $in_series )`), "New bindings" (`ttm/section-label {"format":"more-in"}` → "More in {primary category name}" via `Values::section_label( string $format, string $name )`; '' without a name), rule 26 (unit tests for normal and empty values), rule 40 (theme adds no data call), "Colour vs a11y" (`box-btn` background accent-700). Pattern more-in: `div.ttm-more-in` (`layout: default`) > `div.ttm-cell-heading.is-rail` > `core/heading {"level":3,"className":"ttm-cell-heading__label","metadata":{"bindings":{"content":{"source":"ttm/section-label","args":{"format":"more-in"}}}}}` with static fallback text "More in"; query unchanged; rows `div.ttm-item` > `core/post-title {"isLink":true,"level":4}`. CSS: `.ttm-more-in .ttm-item { padding: 10px 0; border-top: 0; border-bottom: var(--ttm-rule-1) }`, `.ttm-more-in .ttm-item a { font-size: 14px; font-weight: 600; line-height: 1.35 }`; remove `.ttm-more-in { margin-top: 40px }` (the aside gap owns spacing). Box: title paragraph bound to `ttm/newsletter-copy` (fallback text "The weekly issue."); `.ttm-newsletter-box { padding: 16px 18px }`; title 15/800 margin 0 0 8px; copy 13px neutral-800 margin 0 0 12px; `.ttm-newsletter-box form { display: flex; gap: 6px }` (override the 8px shared rule for the box placement only); button `.btn-primary` accent-700 bg / bg text; ≤ 720 form column, button `.btn-block`, box margin-bottom 24px. ≤ 1024 order is DOM order (main then aside) — assert by bounding boxes.
**Acceptance tests:** un-fixme the nine rows; `ValuesTest`: `test_newsletter_title_by_context`, `test_section_label_more_in_and_empty`; `ArticleSourcesTest`: `test_newsletter_copy_binding_reads_series_context`, `test_section_label_binding_reads_primary_category`; `ArticleTemplatesTest::test_more_in_heading_is_one_label_with_category_name`; existing `test_f13_more_in_section_marks_empty_when_no_other_posts` green.
**Out of scope:** Provider logic; poster.
**Verification:** `npm run lint`; `composer test:unit`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P1-05

### P1-07: Phase 1 push — article screenshots
**Goal:** Regenerate the screenshots, verify every article row passes at 1280 and 390, push, and record the manual check.
**Files touched:** `docs/feedback/phase-3/*.png`, `scripts/css-coverage-allow.txt` (no `P1-` pending line may remain), `tests/e2e/selectors-allow.txt` (same), `docs/PROGRESS.md`.
**Design constraints:** SPEC §8 Phase 1 "Visible result", rule 34 amendment (a phase's push task removes its pending lines), §6.9 rows `bar-*`, `art-*`, `prevnext-*`, `aside-*`, `toc-*`, `more-*`, `box-*` all un-fixme'd.
**Acceptance tests:** `grep -c 'P1-' scripts/css-coverage-allow.txt tests/e2e/selectors-allow.txt` → 0 each; `grep -c '// P1-' tests/e2e/fidelity.spec.mjs` → 0; full `verify`, `npm run test:integration`, `npm run test:e2e` green.
**Out of scope:** Any code change beyond removing stale pending lines.
**Verification:** `npx wp-env run cli wp ttm seed --reset && npm run test:e2e && npm run screenshots`; commit; `git push`. Log: `Manual check: NOT VERIFIED (human) — compare docs/feedback/phase-3/article.png with docs/feedback/design_article.png and article-390.png with mock 3b (docs/Eric Mann Newspaper.dc.html lines 118–163)`.
**Depends on:** P1-06

## Phase 2 — Journal post and journal archive

### P2-01: Journal post header, body column, note column and syndication line (§6.4)
**Goal:** Lay out `single-journal.html` as the mock's 3/7/2 grid with the big date, weekday sub-line, 36em body, syndication line and the standing note.
**Files touched:** `themes/ttm-theme/templates/single-journal.html`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.12 journal post header */`, `/* 4.13 syndication line */`), `plugins/ttm-core/blocks/syndicated-to/render.php`, `plugins/ttm-core/src/Bindings/Sources.php` (`ttm/word-count` `whenUnsyndicated`; `drop_empty_bound()`), `tests/unit/Bindings/ArticleValuesTest.php`, `tests/integration/Bindings/ArticleSourcesTest.php`, `tests/integration/Blocks/SyndicatedToTest.php`, `tests/integration/Theme/ArticleTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `jr-grid`, `jr-kicker`, `jr-date`, `jr-date-phone`, `jr-sub`, `jr-h1`, `jr-body`, `jr-body-p`, `jr-synd`, `jr-synd-link`, `jr-synd-words`, `jr-note`, `jr-rss`, `jr-count-hidden`, `jr-rule`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.4 first three bullets and ≤ 1024/≤ 720 bullets, F14, rule 36 extended (the three column groups `layout: default`), Decision "Empty bound blocks" (`drop_empty_bound()` on `render_block_core/paragraph` and `render_block_core/heading`: a `ttm/*`-bound block whose stripped text is empty renders ''), `ttm/word-count` arg `whenUnsyndicated: true` → '' when `ttm_syndication` has any non-empty URL, rule 44 (`--article-h-2`). Template: `div.is-style-grid-3-7-2.ttm-journal-head` (`layout: default`) > col 1 group (`layout: default`): kicker `p.is-style-kicker` "Journal", `p.is-style-journal-date` bound `ttm/short-date`, `p.ttm-journal-head__subline` bound `ttm/journal-subline`; `main` (`layout: default`): `core/post-title {"level":1,"className":"is-style-journal-title"}`, `core/post-content`, `ttm/syndicated-to`; col 3 group (`layout: default`): `p.ttm-journal-head__note` "Journal entries are short and unpolished — things I saw and what they made me think. Longer arguments land in a section.", `p.ttm-journal-head__rss` with the link "Journal RSS" to `/category/journal/feed/`, `p.ttm-journal-head__count` bound `ttm/word-count {"whenUnsyndicated":true}`. Then `hr.is-style-rule-2`, then the stream pattern (P2-02), then footer. Syndication render: `div.ttm-syndication > span.ttm-syndication__text + span.ttm-syndication__words` (no `<p>`). CSS: head grid `gap: 0 48px; padding: 40px 0 48px`; kicker margin 0 0 6px; date 48px/1/−0.025em/800 tnum margin 0 0 0 −0.03em; subline 14px neutral-700 margin 6px 0 0; main `max-width: 36em`; h1 30px/1.12/−0.015em/800 margin 0 0 18px; `.ttm-journal-head main .entry-content p { margin: 0 0 18px }` (last-child 0); syndication flex gap 20px align center margin-top 28px padding-top 14px `border-top: var(--ttm-rule-1)` 13px neutral-700, links accent-700, `__words` margin-left auto; note 12px/1.5 neutral-700 margin 0 0 8px; rss link 12px accent-700; count 12px neutral-700. ≤ 1024: single column; note column becomes a 1px-ruled footnote (padding-top 14px, `border-top: var(--ttm-rule-1)`). ≤ 720: date 36px, h1 26px, body 17/1.6. Remove `.ttm-journal-head:has(.ttm-syndication) .ttm-journal-head__count` (server-side now).
**Acceptance tests:** un-fixme the fifteen `jr-*` rows; `ArticleValuesTest::test_word_count_when_unsyndicated_flag`; `ArticleSourcesTest`: `test_word_count_binding_is_empty_when_post_is_syndicated`, `test_empty_bound_paragraph_renders_nothing`; `SyndicatedToTest` updated to spans; `ArticleTemplatesTest`: `test_journal_head_columns_are_not_constrained_and_note_has_mock_copy`, existing `test_f14_journal_without_syndication_shows_word_count_in_note_column` green.
**Out of scope:** The stream (P2-02); journal archive (P2-03).
**Verification:** `npm run lint`; `composer test:unit`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P1-07

### P2-02: Journal stream (01 §4.11) with whole-row links and "Full journal · N entries"
**Goal:** Render the "Earlier" stream as four 3/7/2 rows that are single links with the mock's type, and the heading link "Full journal · {N} entries".
**Files touched:** `themes/ttm-theme/patterns/journal-stream.php`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.11 journal stream row */`), `plugins/ttm-core/src/Blocks/Helpers.php` (`link_rows()` + registration on `render_block_core/group`), `plugins/ttm-core/src/Bindings/Values.php` and `Sources.php` (`category-count` format `journal-full`), `tests/unit/Blocks/HelpersTest.php`, `tests/unit/Bindings/ValuesTest.php`, `tests/integration/Theme/ArticleTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `js-head`, `js-link`, `js-row`, `js-date`, `js-title`, `js-excerpt`, `js-words`, `js-count`, `js-phone`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.4 "stream" bullet and ≤ 1024/≤ 720 (rows `auto 1fr`, date 16px/800, no word count), Decisions "Whole-row links" (`link_rows()`: group `className` contains `ttm-journal-row` or `ttm-archive-row` and `$block->context['postId']` → outer `<div` → `<a href="{permalink}"`, closing `</div>` → `</a>`; inner `core/post-title` `"isLink":false`), "New bindings" (`journal-full` → "Full journal · {N} entries"; 0 → "All →"), "Colour vs a11y" (`js-words` neutral-700), rule 33 (one anchor per row, headline first in the accessible name: put the title block before the date in the DOM and place the date visually with `grid-column: 1; grid-row: 1`). Pattern: `div.ttm-journal-stream` (`layout: default`) > `div.ttm-cell-heading` (no `is-rail`; `margin-bottom: 8px`) > label "Earlier" + link paragraph bound `ttm/category-count {"category":"journal","format":"journal-full"}` (12px neutral-700); query `perPage` 4 (`journal.stream_count` stays the plugin default; the pattern literal is markup) excluding current; row `div.ttm-journal-row` (`layout: default`) > `core/post-title {"isLink":false,"level":3}`, `core/paragraph.is-style-journal-stream-date` bound `ttm/short-date`, `core/post-excerpt`, `p.ttm-journal-row__words` bound `ttm/word-count`. CSS: `.ttm-journal-stream { padding: 24px 0 40px }`; row grid `minmax(0,3fr) minmax(0,7fr) minmax(0,2fr)` gap 0 48px padding 18px 0 `border-top: var(--ttm-rule-1)` align baseline, `grid-template-rows: auto auto`; date col 1 row 1 20px/800/−0.015em tnum; title col 2 row 1 18px/800/1.25; excerpt col 2 row 2 15px/1.5 neutral-800 `max-width: 40em` margin-top 4px; words col 3 row 1 12px neutral-700 `justify-self: end`; whole row `color: inherit; text-decoration: none`, hover title accent. ≤ 1024: grid `auto 1fr`, date 16px, words `display: none`. Zero rows → the pattern group is hidden via `.ttm-journal-stream:not(:has(.wp-block-post)) { display: none }`.
**Acceptance tests:** un-fixme the nine `js-*` rows; `HelpersTest::test_link_rows_turns_row_group_into_anchor` and `::test_link_rows_ignores_other_groups`; `ValuesTest::test_category_count_journal_full_format` (and zero → "All →"); `ArticleTemplatesTest`: `test_journal_stream_rows_are_single_anchors_with_four_entries`, `test_journal_stream_heading_reads_full_journal_count`.
**Out of scope:** Archive rows' CSS (P3-03) — `link_rows()` already covers their class, which P3-03 relies on.
**Verification:** `npm run lint`; `composer test:unit`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P2-01

### P2-03: Journal archive (`category-journal.html`)
**Goal:** Render `/category/journal/` as the archive header followed by the full-width stream rows (20 per page) with pagination and no filter row or aside.
**Files touched:** `themes/ttm-theme/templates/category-journal.html`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.11 */` archive variant only), `tests/integration/Theme/ArchiveTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `aj-rows`, `aj-no-filter`), `tests/e2e/specs/phone.spec.mjs` (journal post and journal archive: no horizontal overflow at 390).
**Design constraints:** SPEC §6.6 "Journal archive", §6.4 rows, rule 36 extended (`main` `layout: default`), Decision "Whole-row links". Template: header-inner · `ttm/archive-header` pattern (P3-01 restyles it; this task only keeps it) · `hr.is-style-rule-2` · `main` (`layout: default`) > `core/query {"perPage":20,"inherit":true}` with the same row markup as P2-02's stream row (`ttm-journal-row`, title first, `isLink: false`) · `core/query-pagination` with previous/next only (no numbers) · footer. Pagination styling lands in P3-03; nothing to assert here.
**Acceptance tests:** un-fixme `aj-rows`, `aj-no-filter`; `ArchiveTemplatesTest::test_category_journal_renders_stream_rows` updated (rows are anchors, ≥ 9 rows, no `.ttm-filter-row`, no `.ttm-archive-body`); phone.spec: `journal post and archive have no horizontal overflow at 390`.
**Out of scope:** Archive header styling (P3-01); pagination CSS (P3-03).
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P2-02

### P2-04: Phase 2 push — journal screenshots
**Goal:** Regenerate the screenshots, confirm every `jr-*`, `js-*`, `aj-*` row passes, push, record the manual check.
**Files touched:** `docs/feedback/phase-3/*.png`, `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`, `docs/PROGRESS.md`.
**Design constraints:** SPEC §8 Phase 2 "Visible result"; rule 34 amendment (no `P2-` pending line remains).
**Acceptance tests:** `grep -c 'P2-' scripts/css-coverage-allow.txt tests/e2e/selectors-allow.txt` → 0; `grep -c '// P2-' tests/e2e/fidelity.spec.mjs` → 0; full `verify`, integration and e2e green.
**Out of scope:** Code changes beyond stale pending lines.
**Verification:** `npx wp-env run cli wp ttm seed --reset && npm run test:e2e && npm run screenshots`; commit; `git push`. Log: `Manual check: NOT VERIFIED (human) — compare docs/feedback/phase-3/journal.png with docs/feedback/design_journal.png (date block left, 36em body, syndication line, "Earlier" stream)`.
**Depends on:** P2-03

## Phase 3 — Archives, search, 404, page

### P3-01: Archive header (01 §4.25) and the `ttm/archive-kind` kicker
**Goal:** Render the archive header as the mock's 8/4 grid with kicker, 80px H1, description and the stats column, on category, tag and date archives.
**Files touched:** `themes/ttm-theme/patterns/archive-header.php`, `themes/ttm-theme/templates/archive.html`, `themes/ttm-theme/templates/category.html`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.25 archive head */`), `plugins/ttm-core/blocks/category-stats/render.php` (class names only if needed), `plugins/ttm-core/src/Bindings/Sources.php` and `Values.php` (`ttm/archive-kind`), `tests/unit/Bindings/ValuesTest.php`, `tests/integration/Bindings/ArticleSourcesTest.php` (or a new `ArchiveSourcesTest.php`), `tests/integration/Theme/ArchiveTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `ar-head`, `ar-kicker`, `ar-h1`, `ar-h1-phone`, `ar-desc`, `ar-stats`, `ar-stats-text`, `ar-stats-rss`, `tag-kicker`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.6 "Archive header", "Tag / date archive", Decision S5 (`ttm/archive-kind`: pure `Values::archive_kind( array $flags )` mapping `category|tag|month|year|day|author|search` → "Section"/"Tag"/"Month"/"Year"/"Day"/"Author"/"Search", '' otherwise; the pattern paragraph's static text "Section" is the plugin-inactive fallback), rule 36 extended (left column group `layout: default`; header group `is-style-grid-8-4 ttm-archive-head` `layout: default`), rule 26. `archive.html` uses the shared pattern (drop its own header group); keep its rows/aside as they are until P3-03/04. CSS: head `column-gap: 64px; padding: 40px 0 28px; align-items: end`; kicker margin 0 0 10px; h1 (`.ttm-archive-head h1`, the `core/query-title` with `is-style-display-xl`) 80px/.95/−0.03em/800 margin 0 0 14px −0.058em; description p 17/1.5 neutral-800 `max-width: 52ch` margin 0; `.ttm-category-stats` 13px/1.6 neutral-700 tnum, links accent-700, `p { margin: 0 }`. ≤ 720: stacks, h1 44px.
**Acceptance tests:** un-fixme the nine rows; `ValuesTest::test_archive_kind_labels_and_empty`; integration `test_archive_kind_binding_on_category_tag_and_month`; `ArchiveTemplatesTest::test_tag_archive_header_reads_tag_kicker_and_no_stats`.
**Out of scope:** Filter row, body, aside, search.
**Verification:** `npm run lint`; `composer test:unit`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P2-04

### P3-02: Filter row (01 §4.26) placed directly, with "All"
**Goal:** Render `ttm/tag-filter` directly under the header with the "All" chip, active state from `?tag=`, and the S6 rule handling.
**Files touched:** `plugins/ttm-core/blocks/tag-filter/render.php`, `themes/ttm-theme/templates/category.html`, `themes/ttm-theme/patterns/filter-row.php` (delete), `themes/ttm-theme/assets/css/ttm.css` (`/* 4.26 filter row */`; remove `.ttm-filter-row-wrap`), `tests/integration/Blocks/TagFilterTest.php`, `tests/integration/Theme/ArchiveTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `ar-filter`, `ar-filter-all`, `ar-filter-active`, `ar-filter-sort`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.6 "Filter row", F16, Decision S6 (the body group carries the 2px rule; `.ttm-filter-row + .ttm-archive-body { border-top: 0 }` — the body class is added by P3-03; add the rule here so P3-03 only adds the class), rule 46 (removing the `ttm-filter-row-wrap` class updates CSS, the lint lists and tests in this commit). Render: after the "Filter" label, `a.tag.tag-accent|tag-neutral` "All" linking to the category (accent when `get_query_var('tag')` is empty, neutral otherwise), then the top tags (`tag-accent` only for the active slug), then `span.ttm-filter-row__sort` "Newest first". CSS: flex gap 6px align center padding 12px 0 `border-top: var(--ttm-rule-2)` `border-bottom: var(--ttm-rule-1)` 12px; label neutral-700 margin-right 8px (no 800 weight); sort neutral-700 margin-left auto (no float); ≤ 720 `overflow-x: auto; white-space: nowrap; scrollbar-width: none`.
**Acceptance tests:** un-fixme the four rows; `TagFilterTest`: `test_all_chip_is_accent_without_tag_and_neutral_with_tag`, existing tests updated; `ArchiveTemplatesTest::test_category_security_renders_header_stats_filter_year_groups_and_aside` updated (no wrap group).
**Out of scope:** Body rows (P3-03).
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e`; `grep -rn ttm-filter-row-wrap themes plugins scripts tests` returns nothing.
**Depends on:** P3-01

### P3-03: Archive body — year groups, rows, meta line and pagination (01 §4.27–4.28)
**Goal:** Render the archive list as ruled year groups of single-link rows with the mock's type and a two-sided pagination with year-range labels and no numbers.
**Files touched:** `themes/ttm-theme/templates/category.html`, `themes/ttm-theme/templates/archive.html`, `themes/ttm-theme/templates/category-journal.html` (pagination block only), `themes/ttm-theme/assets/css/ttm.css` (`/* 4.27 year group */`, `/* 4.28 pagination */`), `plugins/ttm-core/src/Blocks/Helpers.php` (`group_by_year()` emits the year label as `p.ttm-archive-year__label.tnum` inside `div.ttm-archive-year` with the rows `ul.ttm-archive-year__rows`), `plugins/ttm-core/src/Bindings/Sources.php` (`tags_or_series()` " · " join; disabled pagination spans), `plugins/ttm-core/src/Bindings/Values.php` (`pagination_disabled_label( string $dir )`), `tests/unit/Bindings/ValuesTest.php`, `tests/unit/Bindings/PaginationValuesTest.php`, `tests/integration/Blocks/ArchiveByYearTest.php`, `tests/integration/Query/ArchiveTest.php` (if it asserts the meta join), `tests/integration/Theme/ArchiveTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `ar-body`, `ar-year`, `ar-year-label`, `ar-row`, `ar-row-date`, `ar-row-title`, `ar-row-dek`, `ar-row-meta`, `ar-pagination`, `ar-pagination-numbers`, `ar-phone-year`, `tag-aside`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.6 "Body" and ≤ 720 (year label 24px as a 2px-ruled heading above rows; rows `auto 1fr`), F15, rule 36 extended (body group `is-style-grid-8-4 ttm-archive-body` `layout: default`; `main`/`aside` `layout: default`), Decisions "Whole-row links" (rows `div.ttm-archive-row` become anchors via `link_rows()`; title block first, `isLink: false`), "Pagination" (`label_next/previous` return `span.wp-block-query-pagination-next|previous.is-disabled` with "Older →"/"← Newer" when core rendered '' and the query inherits), "Colour vs a11y" (disabled side neutral-700), rule 24. Row markup: `div.ttm-archive-row` (`layout: default`) > `core/post-title {"isLink":false,"level":3,"className":"ttm-archive-row__title"}`, `p.ttm-archive-row__date` bound `ttm/short-date`, `core/post-excerpt {"className":"ttm-archive-row__dek"}`, `p.ttm-archive-row__meta` bound `ttm/meta-line {"parts":["reading","tags-or-series"]}`. CSS: body `column-gap: 64px; padding: 8px 0 48px; border-top: var(--ttm-rule-2)`; `.ttm-archive-year` grid `120px 1fr` gap 0 24px padding 24px 0 8px `border-top: var(--ttm-rule-2)`; label 32px/800/−0.02em tnum line-height 1 margin 0; rows `ul` list-style none margin/padding 0; row grid `72px 1fr` gap 0 20px padding 14px 0 `border-bottom: var(--ttm-rule-1)` `grid-template-rows: auto auto auto`, colour inherit, no underline; date col 1 row 1 12px neutral-700 tnum padding-top 4px; title col 2 row 1 20px/800/1.2 margin 0; dek col 2 row 2 14px/1.45 neutral-800 `max-width: 56ch` margin-top 5px; meta col 2 row 3 12px neutral-700 margin-top 6px; hover title accent. Pagination: `.wp-block-query-pagination { display: flex; justify-content: space-between; padding: 20px 0; font-size: 13px; font-weight: 600; border-top: 0; margin: 0 }`, `.is-disabled` neutral-700; `core/query-pagination-numbers` removed from all three templates; the `.ttm-archive-year__label` phone rule (24px, `border-bottom: var(--ttm-rule-2)`, padding-bottom 8px, grid single column, rows `auto 1fr`). `tag-aside`: `archive.html`'s aside has `ttm/most-read` only (styled in P3-04); its `.ttm-series-list` count 0 holds already.
**Acceptance tests:** un-fixme the twelve rows; `ValuesTest::test_meta_line_tags_joined_with_middle_dots`; `PaginationValuesTest::test_disabled_labels`; integration `test_pagination_missing_side_renders_disabled_span` and `ArchiveByYearTest` updated to the label element; `ArchiveTemplatesTest::test_archive_row_is_single_link_with_title_name` rewritten (row is an `<a class="… ttm-archive-row …" href=…>`, one anchor per row, title text first) and `test_category_page_two_has_newer_label_with_years` green.
**Out of scope:** Aside (P3-04); search/404/page (P3-05).
**Verification:** `npm run lint`; `composer test:unit`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P3-02

### P3-04: Archive aside — series rail, most read (01 §4.29) and the tablet grid
**Goal:** Render the aside as "Series in {Section}" rail rows and the numbered "Most read", with the two-column aside at ≤ 1024.
**Files touched:** `themes/ttm-theme/templates/category.html`, `plugins/ttm-core/blocks/series-list/render.php` and `block.json` (`layout` enum `list|rail|grid-2|grid-3|strip`), `plugins/ttm-core/blocks/most-read/render.php`, `themes/ttm-theme/templates/page-writing.html`, `themes/ttm-theme/templates/taxonomy-series.html` (only the `layout` attribute value `rows` → `list`), `themes/ttm-theme/assets/css/ttm.css` (`/* 4.17 series row */` rail rules, `/* 4.29 numbered rows */`), `plugins/ttm-core/src/Bindings/Sources.php` (`ttm/section-label {"format":"series-in"}`), `tests/unit/Bindings/ValuesTest.php`, `tests/integration/Blocks/SeriesListTest.php`, `tests/integration/Blocks/MostReadTest.php`, `tests/integration/Theme/ArchiveTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `ar-aside-series`, `ar-aside-row`, `ar-mostread`, `ar-mostread-num`, `ar-tablet-aside`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.6 "Right aside", ≤ 1024 bullet, Decisions "`ttm/series-list` layouts" (restated: `rows` removed; `rail` = mark + `__title` + `__meta` (categories · count · cadence); `list` = mark, `__title`, `__dek`, `__meta` form line, `__count`; every template/test naming `rows` updated now, `list` CSS lands in P4-05), "`ttm/most-read`" (`div.ttm-cell-heading.is-rail > p.ttm-cell-heading__label` "Most read"; `ol.ttm-numbered > li.ttm-numbered__row > span.ttm-numbered__num.tnum + a`), "New bindings" (`series-in` → "Series in {queried category name}"), "Colour vs a11y" (numbers neutral-700), rule 46. Template aside (`layout: default`): `div.ttm-cell-heading.is-rail` > heading bound `ttm/section-label {"format":"series-in"}` (fallback "Series in"); `ttm/series-list {"inCategory":true,"layout":"rail","limit":4}`; `ttm/most-read`. CSS: aside `padding-top: 24px; display: flex; flex-direction: column; gap: 28px`; `.ttm-series-list.is-rail .ttm-series-row` grid `10px 1fr` gap 12px padding 12px 0 `border-bottom: var(--ttm-rule-1)` (no top rule), mark margin-top 5px, title 15/800/1.25, meta 12px neutral-700 margin-top 3px; `.ttm-numbered` list-style none margin/padding 0; `.ttm-numbered__row` grid `24px 1fr` gap 8px padding 10px 0 `border-bottom: var(--ttm-rule-1)` 14px/600/1.35; `__num` 800 neutral-700 tnum; `.ttm-most-read .ttm-cell-heading` is-rail. ≤ 1024: `.ttm-archive-body aside { display: grid; grid-template-columns: 1fr 1fr; gap: 0 32px }`; ≤ 720 single column. Remove the dead `.ttm-most-read__list/__item/__num` rules.
**Acceptance tests:** un-fixme the five rows; `ValuesTest::test_section_label_series_in`; `SeriesListTest`: `test_rail_layout_renders_title_and_meta_only`, `test_layout_rows_is_no_longer_accepted` (block.json enum), existing tests updated; `MostReadTest` updated to the numbered classes; `ArchiveTemplatesTest::test_category_aside_reads_series_in_section_and_numbered_most_read`.
**Out of scope:** `list`/`grid-2` CSS (Phase 4).
**Verification:** `npm run lint`; `composer test:unit`; `npm run test:integration`; `npm run test:e2e`; `grep -rn '"layout":"rows"' themes plugins tests` returns nothing.
**Depends on:** P3-03

### P3-05: Search, 404 and static page (02 §H)
**Goal:** Build `search.html`, `404.html` and `page.html` from the system per §6.6's last three bullets.
**Files touched:** `themes/ttm-theme/templates/search.html`, `themes/ttm-theme/templates/404.html`, `themes/ttm-theme/templates/page.html`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.24 */` page, `/* 4.29 */` latest counters, search form, 404), `plugins/ttm-core/src/Blocks/Helpers.php` (`style_search()`), `plugins/ttm-core/src/Bindings/Sources.php` and `Values.php` (`ttm/search-summary`), `tests/unit/Blocks/HelpersTest.php`, `tests/unit/Bindings/ValuesTest.php`, `tests/integration/Theme/ArchiveTemplatesTest.php`, `tests/integration/Theme/TemplatesShellTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `search-h1`, `search-form`, `search-row-kicker`, `404-h1`, `404-strip`, `404-latest`, `page-grid`, `page-h1`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.6 "Search", "404", "Static page", Decisions "Search block classes", "New bindings" (`ttm/search-summary`: `Values::search_summary( string $query, int $found )` → "Results for “{q}”" / "Nothing matched “{q}”."; '' when not `is_search()`), "404 Latest numbers" (CSS counters), "About page image" (`core/post-featured-image {"className":"ttm-page__portrait"}` above the title; 3:2, colour), rule 36 extended, rule 40. `search.html`: header group `is-style-grid-8-4 ttm-archive-head` (`layout: default`) > left column: `p.is-style-kicker` bound `ttm/archive-kind`, `h1.is-style-display-xl` "Search", `p.is-style-dek.ttm-archive-head__summary` bound `ttm/search-summary`, `core/search {"showLabel":false,"buttonText":"Search","className":"ttm-search"}`; body `is-style-grid-8-4 ttm-archive-body` with `main` only (no aside; CSS `.ttm-archive-body:not(:has(aside)) { grid-template-columns: minmax(0, 8fr) minmax(0, 4fr) }` is not needed — keep the grid and leave the second track empty); rows = P3-03's archive row plus `core/post-terms {"term":"category","className":"is-style-kicker"}` before the title (kicker 12px accent-700 `grid-column: 2`); pagination as P3-03; a `core/query-no-results` is not added (zero results = header only). `404.html`: `main` (`layout: default`) > `h1.is-style-display-xl` "Not here.", `p.is-style-dek` (17px) "The page you're looking for doesn't exist. Try a search.", `core/search` as above, `ttm/series-strip` pattern (unchanged phase 2 markup), `div.ttm-latest` > `div.ttm-cell-heading.is-rail` "Latest" + `core/query {"perPage":4,"inherit":false,"namespace":"ttm/section-query"}` rows `div.ttm-numbered__row` (`layout: default`) > `core/post-title {"isLink":true,"level":3}` + `core/post-date {"className":"ttm-numbered__date"}`. `page.html`: `main.is-style-grid-8-4` (`layout: default`; padding 40px 0 48px) > column group (`layout: default`) > `core/post-featured-image {"className":"ttm-page__portrait"}`, `core/post-title {"level":1}`, `core/post-content` (core adds `entry-content`); empty aside. CSS: `.ttm-search` form flex gap 6px, input 320px; `.ttm-latest .ttm-numbered__row` 17px/800 with date 12px neutral-700, counters; `.ttm-page__portrait` `aspect-ratio: 3/2`, margin-bottom 28px; `main.is-style-grid-8-4 h1` 56px margin 0 0 18px.
**Acceptance tests:** un-fixme the eight rows; `HelpersTest::test_style_search_adds_input_and_button_classes`; `ValuesTest::test_search_summary_with_and_without_results`; `ArchiveTemplatesTest::test_search_template_renders_query_and_rows` updated (H1 "Search", summary text, kicker per row) plus `test_search_with_no_results_shows_nothing_matched`; `TemplatesShellTest::test_404_template_renders_not_here_and_search` updated (strip + four latest rows) and `test_page_template_has_main_landmark_and_aside` green.
**Out of scope:** Newsletter landing page; comments.
**Verification:** `npm run lint`; `composer test:unit`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P3-04

### P3-06: Phase 3 push — archive screenshots
**Goal:** Regenerate the screenshots, confirm every `ar-*`, `tag-*`, `search-*`, `404-*`, `page-*` row passes, push, record the manual check.
**Files touched:** `docs/feedback/phase-3/*.png`, `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`, `docs/PROGRESS.md`.
**Design constraints:** SPEC §8 Phase 3 "Visible result"; rule 34 amendment (no `P3-` pending line remains).
**Acceptance tests:** `grep -c 'P3-' scripts/css-coverage-allow.txt tests/e2e/selectors-allow.txt` → 0; `grep -c '// P3-' tests/e2e/fidelity.spec.mjs` → 0; full `verify`, integration and e2e green.
**Out of scope:** Code changes beyond stale pending lines.
**Verification:** `npx wp-env run cli wp ttm seed --reset && npm run test:e2e && npm run screenshots`; commit; `git push`. Log: `Manual check: NOT VERIFIED (human) — compare docs/feedback/phase-3/archive-security.png with mock 1e (docs/Eric Mann Newspaper.dc.html lines 917–975): 80px "Security", filter chips, two year groups, "Series in Security" and "Most read" rail; search.png and 404.png against 02 §H`.
**Depends on:** P3-05

## Phase 4 — Series hub, single series, Writing

### P4-01: Series hub header and featured block (01 §4.38, §6.7)
**Goal:** Render `/series/` as the mock's header (H1, description, stats) and the 5/7 featured block with progress, buttons and the ruled part list.
**Files touched:** `themes/ttm-theme/templates/page-series.html`, `plugins/ttm-core/blocks/series-featured/render.php` and `block.json` (`showDek`), `plugins/ttm-core/blocks/series-progress/render.php` (classes only if needed), `themes/ttm-theme/assets/css/ttm.css` (`/* 4.38 series featured */`, `/* 4.19 series progress */`, hub head), `tests/integration/Blocks/SeriesFeaturedTest.php`, `tests/integration/Theme/HubWritingTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `hub-head`, `hub-h1`, `hub-desc`, `hub-stats`, `hub-featured`, `hub-kicker`, `hub-title`, `hub-dek`, `hub-progress`, `hub-progress-color`, `hub-meta`, `hub-buttons`, `hub-parts`, `hub-part`, `hub-part-num`, `hub-part-scheduled`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.7 "Header", "Featured", F5, F24, rule 36 extended (`main` `layout: default`; `ttm-hub-head` left column `layout: default`), Decisions "`ttm/series-featured`" (classes `__main`, `__kicker`, `__title`, `__dek`, `__buttons`, `__parts`, `__part.is-published|is-scheduled`, `__num`, `__part-title`, `__part-dek`, `__date`, `__all`; categories joined " · "; `h1` when `is_tax('series')` else `h2`; `showDek`), "Colour vs a11y" (`__num` neutral-700). Template: header-inner · `main` (`layout: default`) > `div.is-style-grid-8-4.ttm-hub-head` (`layout: default`) > column (`layout: default`) [`h1.is-style-display-xl` "Series", `p.is-style-dek` "Longer arguments, split across posts and sometimes across sections. Each series has an order, a status, and a single page that holds it together."] + `ttm/series-stats` · `hr.is-style-rule-2` · `ttm/series-featured` · `hr.is-style-rule-2` · `div.ttm-hub-all` (P4-02) · footer (newsletter box removed in P4-02). CSS: hub head `column-gap: 64px; padding: 40px 0 28px; align-items: end`; h1 80px margin 0 0 14px −0.058em; `.ttm-hub-head .is-style-dek` 17/1.5 neutral-800 `max-width: 52ch` margin 0; `.ttm-series-stats` 13px/1.6 neutral-700 tnum `p { margin: 0 }`; featured grid `minmax(0,5fr) minmax(0,7fr)` `column-gap: 64px` padding 28px 0 36px; kicker margin 0 0 10px; title 40px/1.02/−0.02em/800 margin 0 0 12px; dek 16/1.5 neutral-800 margin 0 0 16px; progress segments flex gap 4px margin-bottom 8px, `__seg` `flex: 1; height: 6px` (`is-done` neutral-900, `is-todo` neutral-300); `__meta` 12px neutral-700 tnum margin 0 0 18px; buttons flex gap 10px margin 0; parts `ol` list-style none margin/padding 0 `border-top: var(--ttm-rule-2)`; part grid `40px 1fr auto` gap 16px padding 12px 0 `border-bottom: var(--ttm-rule-1)` align baseline 16px/1.3; num 800 neutral-700 tnum; `.is-published .ttm-series-featured__part-title` 800 text; `.is-scheduled .ttm-series-featured__part-title` 400 neutral-700; date 12px neutral-700; `__all` 13px accent-700. ≤ 1024 featured stacks; ≤ 720 h1 44px, part rows `28px 1fr` with the date on a second line (`grid-column: 2`).
**Acceptance tests:** un-fixme the sixteen rows; `SeriesFeaturedTest`: existing tests updated to the classes, `test_kicker_joins_categories_with_middle_dots`, `test_title_is_h1_on_series_archive_and_h2_elsewhere`, `test_show_dek_renders_part_excerpts_for_published_parts_only`; `HubWritingTemplatesTest::test_series_index_page_renders_stats_featured_and_all_series_grid` updated.
**Out of scope:** All-series grid (P4-02); single series (P4-03).
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P3-06

### P4-02: All-series grid (`layout=grid-2`) and hub clean-up
**Goal:** Style the "All series" two-column index rows and remove the hub's newsletter box.
**Files touched:** `themes/ttm-theme/templates/page-series.html`, `plugins/ttm-core/blocks/series-list/render.php` (`grid-2` emits `__categories`; count column), `themes/ttm-theme/assets/css/ttm.css` (`/* 4.17 series row */` grid-2 and list-shared rules), `tests/integration/Blocks/SeriesListTest.php`, `tests/integration/Theme/HubWritingTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `hub-all-head`, `hub-grid`, `hub-grid-row`, `hub-grid-title`, `hub-grid-cats`, `hub-nobox`, `hub-phone`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.7 "All series", ≤ 1024 (single column), Decision "`ttm/series-list` layouts" (`grid-2` rows: mark, `__title`, `__dek`, `__categories`, `__count` [`__parts` over `__status`]), "Colour vs a11y" (`__status` complete/hiatus neutral-700, in-progress accent-700 600). Template: `div.ttm-hub-all` (`layout: default`) > `div.ttm-cell-heading` (no rule, `margin-bottom: 12px`) > label "All series" + `p.ttm-cell-heading__link` "Sorted by last update" (12px neutral-700); `ttm/series-list {"status":"any","limit":50,"layout":"grid-2"}`; delete the newsletter-box pattern reference. CSS: `.ttm-series-list.is-grid-2` grid `1fr 1fr` gap 0 48px; its rows grid `10px 1fr auto` gap 0 14px padding 18px 0 `border-top: var(--ttm-rule-1)` align start; mark margin-top 6px; title 20/800/1.15/−0.01em; dek 14/1.45 neutral-800 `max-width: 44ch` margin-top 5px; categories 12px neutral-700 margin-top 8px; count 12px neutral-700 tnum right-aligned line-height 1.5 with `__status` 600 coloured by status. Delete the `.ttm-hub-all.ttm-hub-all` double-class hack and the old generic `.ttm-series-row` sizes that conflict (keep the strip rules and phase 2 rows green). ≤ 720 single column.
**Acceptance tests:** un-fixme the seven rows; `SeriesListTest`: `test_grid_2_layout_renders_dek_categories_and_count`, `test_categories_are_joined_with_middle_dots_in_rows_layout` renamed for `grid-2`; `HubWritingTemplatesTest::test_series_index_has_no_newsletter_box`.
**Out of scope:** `list` layout CSS (P4-05).
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P4-01

### P4-03: Single series template (`taxonomy-series.html`) and `series.related_limit`
**Goal:** Render a single series as the featured block's left column full width, the full ruled part list with dek lines, and "Other series".
**Files touched:** `themes/ttm-theme/templates/taxonomy-series.html`, `plugins/ttm-core/blocks/series-list/render.php` (`related_limit` fallback), `themes/ttm-theme/assets/css/ttm.css` (`/* 4.38 */` single-series variant), `tests/integration/Blocks/SeriesListTest.php`, `tests/integration/Theme/HubWritingTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `single-head`, `single-kicker`, `single-parts`, `single-other`, `single-nav`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.7 "Single series", §5 (`series.related_limit` read as `Config::get( 'series.related_limit', 4 )` when `excludeCurrent` and `limit` ≤ 0), rule 36 extended, Decision "`ttm/series-featured`" (`h1` on the taxonomy archive; `showDek: true`), Nav (`/series/` current on `is_tax('series')`, already). Template: header-inner · `main.ttm-series-single` (`layout: default`) > `ttm/series-featured {"partsLimit":0,"showDek":true}` · `hr.is-style-rule-2` · `div.ttm-series-single__other` (`layout: default`) > `div.ttm-cell-heading.is-rail` "Other series" + `ttm/series-list {"status":"any","layout":"list","excludeCurrent":true}` (no `limit`) · footer. CSS: `.ttm-series-single .ttm-series-featured { grid-template-columns: 1fr; padding: 40px 0 36px }`; `.ttm-series-single .ttm-series-featured__title` 80px/.95/−0.03em margin 0 0 14px −0.058em `max-width: 16ch`; `.ttm-series-single .ttm-series-featured__part` 18px; `__part-dek` 14px neutral-800 `grid-column: 2`; `.ttm-series-single__other` padding 24px 0 48px. ≤ 720 h1 44px.
**Acceptance tests:** un-fixme the five rows; `SeriesListTest::test_exclude_current_without_limit_uses_related_limit`; `HubWritingTemplatesTest::test_single_series_renders_full_part_list_and_other_series_excluding_itself` updated (h1, dek lines = published parts, ≤ 4 other rows).
**Out of scope:** Writing page.
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P4-02

### P4-04: Writing hero (01 §4.36–4.37, §6.5)
**Goal:** Render `ttm/serial-hero` as the mock's cover-plus-text hero with synopsis, three buttons and the stat row.
**Files touched:** `plugins/ttm-core/blocks/serial-hero/render.php`, `themes/ttm-theme/assets/css/ttm.css` (`/* 4.36 book cover */`, `/* 4.37 stat row */`, `/* 4.38 */` hero rules moved under a `/* 02 §D writing hero */` header or kept under 4.38 with a comment), `tests/integration/Blocks/SerialHeroTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `wr-hero`, `wr-cover`, `wr-kicker`, `wr-title`, `wr-synopsis`, `wr-buttons`, `wr-stats`, `wr-stat-value`, `wr-stat-cadence`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.5 "Hero", F3/F21, rule 46 (`div.ttm-serial-hero[data-ttm-block]`), rule 32. Render: cadence rendered capitalised (`ucfirst` on the first letter only, via `mb_strtoupper( mb_substr( … ) )`); stat rows as `p.ttm-stats__item` > `span.ttm-stats__value` + `span.ttm-stats__label` (keep). CSS: hero grid `280px minmax(0, 1fr)` `column-gap: 64px` padding 40px 0 align end; `:not(.is-nocover)` 320px at ≥ 1200; `.ttm-serial-hero .ttm-cover` 2:3, `box-shadow: var(--wp--preset--shadow--cover)`, no filter; `__body` block; kicker margin 0 0 12px; title 64px/.98/−0.03em/800 margin 0 0 16px −0.04em `max-width: 14ch`; synopsis 19/1.45 neutral-800 `max-width: 48ch` margin 0 0 20px; buttons flex gap 10px margin 0 0 22px; `.ttm-stats` grid `repeat(3, auto)` gap 0 40px justify start `border-top: var(--ttm-rule-2)` padding-top 14px 13px neutral-700 tnum; `__value` 20/800/−0.01em text colour block; `__label` 13px. ≤ 1024 cover 200px; ≤ 720 single column, cover 160px, title 40px, buttons column `.btn` full width, values 16px.
**Acceptance tests:** un-fixme the nine rows; `SerialHeroTest::test_cadence_is_capitalised_and_synopsis_is_term_description`; existing F3/F21 tests green.
**Out of scope:** Writing body.
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P4-03

### P4-05: Writing body — all serials (`layout=list`) and recent chapters (numbered)
**Goal:** Render the Writing page's left column as the mock's serial rows and the numbered recent-chapters list.
**Files touched:** `themes/ttm-theme/templates/page-writing.html`, `plugins/ttm-core/blocks/series-list/render.php` (`list` meta line), `plugins/ttm-core/blocks/series-toc/render.php` (chapters heading/link if not finished in P1-05), `themes/ttm-theme/assets/css/ttm.css` (`/* 4.17 */` list rules, `/* 4.29 */` chapters rules, `/* 02 §D writing body */`), `tests/integration/Blocks/SeriesListTest.php`, `tests/integration/Blocks/SeriesTocTest.php`, `tests/integration/Theme/HubWritingTemplatesTest.php`, `tests/e2e/fidelity.spec.mjs` (rows `wr-body`, `wr-serials-head`, `wr-serial-row`, `wr-serial-title`, `wr-serial-dek`, `wr-serial-form`, `wr-serial-count`, `wr-serial-status`, `wr-chapters-head`, `wr-chapter-row`, `wr-chapter-num`, `wr-chapter-title`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.5 "Body" left column, rule 36 extended (`main` and `aside` `layout: default`; body group `is-style-grid-7-5 ttm-writing-body` `layout: default`), Decisions "`ttm/series-list` layouts" (`list` meta line: fiction "{Form} · {genre} · {cadence}" with form labels Novel/Novella/Story cycle; nonfiction: categories joined " · " then cadence; empties omitted), "`ttm/series-toc`" (chapters variant), "Colour vs a11y" (`__num` neutral-700; `__status` complete neutral-700). Template main: `div.ttm-cell-heading.is-rail` > "All serials" + `p.ttm-cell-heading__link` "Newest activity first"; `ttm/series-list {"form":"fiction","status":"any","limit":10,"layout":"list"}`; `ttm/series-toc {"variant":"chapters","order":"desc","limit":4,"showDek":true}` (no `heading` attribute — the block composes it). CSS: `.ttm-writing-body` `column-gap: 64px; padding: 28px 0 48px`; `main`, `aside` flex column, gap 36px / 32px; `.ttm-series-list.is-list .ttm-series-row` grid `10px 1fr auto` gap 0 14px padding 16px 0 `border-bottom: var(--ttm-rule-1)` align start (no top rule); mark margin-top 6px; title 20/800/1.15/−0.01em; dek 14/1.45 neutral-800 `max-width: 46ch` margin-top 4px; meta 12px neutral-700 margin-top 6px; count right 12px neutral-700 tnum, status 600 (accent-700 in progress); `.ttm-series-toc.is-chapters .ttm-numbered__row` grid `44px 1fr auto` gap 0 16px padding 12px 0 `border-bottom: var(--ttm-rule-1)` align baseline; num 15px/800 neutral-700 tnum; title 17/800/1.2; dek 13px neutral-800 margin-top 3px `grid-column: 2`; date 12px neutral-700 tnum; `.ttm-series-toc.is-chapters .ttm-cell-heading__link` 12px neutral-700.
**Acceptance tests:** un-fixme the twelve rows; `SeriesListTest::test_list_layout_form_line_for_fiction_and_nonfiction`; `SeriesTocTest::test_chapters_variant_composes_heading_and_all_link` green; `HubWritingTemplatesTest::test_writing_page_renders_hero_serials_chapters_tiles_and_books` updated.
**Out of scope:** Aside (P4-06).
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P4-04

### P4-06: Writing aside — story tiles, book grid, and the responsive order
**Goal:** Render "Short fiction" tiles with the explainer, "In print" covers with captions, and the ≤ 1024 / ≤ 720 behaviour of the whole Writing page.
**Files touched:** `themes/ttm-theme/templates/page-writing.html`, `plugins/ttm-core/blocks/book-grid/render.php` (`collection` → "Stories"; `__links` class), `plugins/ttm-core/blocks/story-tiles/render.php` (classes only if needed), `themes/ttm-theme/assets/css/ttm.css` (`/* 4.35 story tile */`, `/* 4.36 book cover */`, `/* 02 §D */` responsive), `tests/integration/Blocks/BookGridTest.php`, `tests/integration/Theme/HubWritingTemplatesTest.php`, `tests/e2e/specs/phone.spec.mjs` (`writing page reorders sections at 1000`), `tests/e2e/fidelity.spec.mjs` (rows `wr-tiles`, `wr-tile`, `wr-tile-title`, `wr-tile-meta`, `wr-tiles-note`, `wr-books`, `wr-book-cover`, `wr-book-title`, `wr-phone`), `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`.
**Design constraints:** SPEC §6.5 right column and ≤ 1024/≤ 720 bullets, F19/F20, Decisions "Book form label", "Writing ≤ 1024 order" (`main`/`aside` `display: contents` at ≤ 1024 with `order` 1–4 on `div.ttm-writing-body__serials`, `__stories`, `__chapters`, `__books` — wrap the two main sections in groups with those classes), rule 46. Template aside: `div.ttm-writing-body__stories` (`layout: default`) > `div.ttm-cell-heading.is-rail` "Short fiction" + `ttm/story-tiles` + `p.ttm-story-tiles__note` "Stories without artwork get a typographic tile; a cover, when one exists, replaces it in the same box."; `div.ttm-writing-body__books` > heading "In print" + `ttm/book-grid`. CSS: `.ttm-story-tiles` grid `1fr 1fr` gap 16px padding-top 16px; `.ttm-tile` 4:3 surface padding 16px flex column space-between, title 19/800/1.1/−0.015em, meta 12px neutral-700 tnum, hover accent-700; `__note` 12px neutral-700 margin 10px 0 0; `.ttm-book-grid` `repeat(2, 1fr)` gap 20px padding-top 16px; `.ttm-book .ttm-cover` no shadow; `__title` 14/800/1.25 margin-top 10px; `__meta` 12px neutral-700 margin-top 2px; `__links .btn-ghost` under the meta. ≤ 720 tiles `repeat(auto-fill, minmax(148px, 1fr))`; hero rules from P4-04 apply.
**Acceptance tests:** un-fixme the nine rows; `BookGridTest::test_form_caption_is_translatable` updated for "Stories"; `HubWritingTemplatesTest::test_empty_state_writing_page_omits_tiles_and_books` green; phone.spec test as named.
**Out of scope:** Hero and left column.
**Verification:** `npm run lint`; `npm run test:integration`; `npm run test:e2e`.
**Depends on:** P4-05

### P4-07: Phase 4 push — hub and Writing screenshots
**Goal:** Regenerate the screenshots, confirm every `hub-*`, `single-*`, `wr-*` row passes, push, record the manual check.
**Files touched:** `docs/feedback/phase-3/*.png`, `scripts/css-coverage-allow.txt`, `tests/e2e/selectors-allow.txt`, `docs/PROGRESS.md`.
**Design constraints:** SPEC §8 Phase 4 "Visible result"; rule 34 amendment (no `P4-` pending line remains).
**Acceptance tests:** `grep -c 'P4-' scripts/css-coverage-allow.txt tests/e2e/selectors-allow.txt` → 0; `grep -c '// P4-' tests/e2e/fidelity.spec.mjs` → 0; full `verify`, integration and e2e green.
**Out of scope:** Code changes beyond stale pending lines.
**Verification:** `npx wp-env run cli wp ttm seed --reset && npm run test:e2e && npm run screenshots`; commit; `git push`. Log: `Manual check: NOT VERIFIED (human) — compare docs/feedback/phase-3/series-hub.png with mock 1f (lines 976–1030) and writing.png with docs/feedback/design_serial.png; series-single.png against 02 §F`.
**Depends on:** P4-06

## Phase 5 — Phone, wide, close-out

### P5-01: 390 and 1920 sweep of every screen
**Goal:** Walk every screen at 390 and 1920 against `02 §B–§H` "Responsive" and `3b`, fix what the table did not catch, and pin the facts in `phone.spec.mjs`.
**Files touched:** `themes/ttm-theme/assets/css/ttm.css` (phone/wide rules only), `tests/e2e/specs/phone.spec.mjs`, `tests/e2e/specs/screens.spec.mjs` (a 1920 check: `.wp-site-blocks` width 1280 on every screen), `tests/e2e/fidelity.spec.mjs` (only if a row's expectation was wrong and SPEC agrees — record it).
**Design constraints:** SPEC §6.8, §6.2/§6.4/§6.5/§6.6/§6.7 ≤ 1024 and ≤ 720 bullets, §6.1.1 phone, rule 42 (1920 centred), rule 38 (no CSS change without a test). Checklist per screen at 390: no horizontal overflow (`documentElement.scrollWidth ≤ 390`), masthead is title + "Menu", footer 11px stacked; article: bar compact, hero full-bleed 4:3, `pre` full-bleed, aside zones below prev/next; journal: date 36px, stacked, note as footnote; Writing: hero stacked, buttons block, tiles 2-col; archive: H1 44, year label ruled heading, filter row scrolls; hub: featured stacked, part rows `28px 1fr`. At 1920: every screen's `.wp-site-blocks` is 1280 wide and centred; the poster on `/` spans exactly the container.
**Acceptance tests:** phone.spec: `every seeded screen has no horizontal overflow at 390` (loop over `SCREEN_URLS`), `archive filter row scrolls horizontally at 390`, `article aside zones follow prev/next at 390`; screens.spec: `every seeded screen is a centred 1280 column at 1920`.
**Out of scope:** Desktop rules; new components.
**Verification:** `npm run lint`; `npm run test:e2e`; open three screens on a real phone browser (logged as the manual check in P5-05).
**Depends on:** P4-07

### P5-02: a11y, network, selectors and seed-hero-color rows; guards back to strict
**Goal:** Un-fixme the cross-cutting rows for every screen, empty both allow-lists of pending lines, and return the coverage and fixme guards to strict mode.
**Files touched:** `tests/e2e/fidelity.spec.mjs`, `tests/e2e/selectors-allow.txt`, `scripts/css-coverage-allow.txt` (must end empty), `scripts/check-css-coverage.mjs` (`ALLOW_PENDING = false`), `scripts/check-fixme.mjs` (`ALLOW_TAGGED = false`), `scripts/test/check-css-coverage.test.js`, `scripts/test/check-fixme.test.js`, `themes/ttm-theme/assets/css/ttm.css` (only to delete a dead selector the strict lint reports), `CLAUDE.md` (constraint lines for the two guards).
**Design constraints:** SPEC §6.9 rows `seed-hero-color`, `a11y`, `network`, `selectors`, §3.1 rule 34 amendment (allow-list empty), §3.2 rule 41 (only `# editor block style (04 §3)` lines may remain in `selectors-allow.txt`), rule 33 (axe serious/critical = 0 on every screen at 1280 and 390, excluding `.ttm-poster .btn-ghost` only), rule 6 (same-origin requests only; keep phase 1's Jetpack-host allowance in `network.spec.mjs`, but the fidelity `network` row asserts same-origin strictly — Jetpack is not installed in wp-env). Any a11y failure is fixed at its source (markup or colour) with the fix recorded; no exclusion is added.
**Acceptance tests:** un-fixme `seed-hero-color`, every per-screen `a11y` and `network` test; `npm run lint` with strict guards; `wc -l scripts/css-coverage-allow.txt` → 0; `grep -c pending tests/e2e/selectors-allow.txt` → 0; `grep -c 'test.fixme(' tests/e2e/fidelity.spec.mjs tests/e2e/editors.spec.mjs` → 0.
**Out of scope:** Budget (P5-03); docs.
**Verification:** `npm run lint`; `npm run test:unit`; `npm run test:e2e` (all projects green).
**Depends on:** P5-01

### P5-03: Tuning — `cssBudgetBytes`
**Goal:** Measure `ttm.css` against the 61440-byte budget after every component landed and settle the number once.
**Files touched:** `scripts/check-budget.mjs` (only if the number changes), `CLAUDE.md` (only if the number changes), `themes/ttm-theme/assets/css/ttm.css` (dedup only), `docs/PROGRESS.md`.
**Design constraints:** SPEC §3.1 rule 30 (⚠️ ASSUMPTION 61440; the number lives only in `scripts/check-budget.mjs` and `CLAUDE.md`; this task records the final size). Method: record `stat -c %s themes/ttm-theme/assets/css/ttm.css` before; remove duplicated declarations and superseded rules (phase 1 leftovers the strict coverage lint no longer needs, repeated phone blocks that can be merged under one `@media`); re-measure; if under budget, lower the budget to the measured size rounded up to the next 1024 and record both numbers; if over, raise once to the same rounding and record why. `Measurement:` in the commit body: `before N bytes / budget 61440 → after M bytes / budget B`.
**Acceptance tests:** none new; `npm run lint`, `npm run test:e2e` green (no fidelity regression from the dedup).
**Out of scope:** Any new rule.
**Verification:** `npm run lint`; `npm run test:e2e`.
**Depends on:** P5-02

### P5-04: Handoff, SETUP/README notes and CI check
**Goal:** Write `docs/HANDOFF.md`, update the developer docs for this flight's harness, and confirm CI runs the new suites.
**Files touched:** `docs/HANDOFF.md` (new), `docs/SETUP.md`, `docs/feedback/README.md`, `plugins/ttm-core/README.md` (new bindings and filters), `.github/workflows/ci.yml` (only if a new script is not already covered by `npm run lint`/`test:e2e`).
**Design constraints:** SPEC §1 "Done", §7. HANDOFF lists: what changed per phase, every `Manual check:` line from the log verbatim, measurements (`seed.image_band_angle`, `cssBudgetBytes`, seeded word counts), interpretation choices (from PLAN Decisions plus anything the log recorded), and the spec issues found during the build.
**Acceptance tests:** none new; `npm run lint` green (markdown is not linted; check links by hand).
**Out of scope:** Code.
**Verification:** `git diff --stat`; read the HANDOFF once end to end.
**Depends on:** P5-03

### P5-05: Phase 5 push — final screenshots
**Goal:** Regenerate every screenshot, push, and record the final manual checks.
**Files touched:** `docs/feedback/phase-3/*.png`, `docs/PROGRESS.md`.
**Design constraints:** SPEC §1 "Done", §6.11, §8 Phase 5 "Visible result".
**Acceptance tests:** none new; full `verify`, `npm run test:integration`, `npm run test:e2e` green; `node scripts/check-fixme.mjs` → clean; `scripts/css-coverage-allow.txt` empty.
**Out of scope:** Any code change.
**Verification:** `npx wp-env stop && npx wp-env start && npx wp-env run cli wp ttm seed --reset && npm run test:e2e && npm run screenshots`; commit; `git push`. Log: `Manual check: NOT VERIFIED (human) — open http://localhost:8888/signing-your-options-table/, /journal-post-1/, /writing/ and /category/security/ at 390 in a real phone browser and compare with mock 3b and the 02 Responsive bullets; confirm CI is green on the branch including the e2e job; compare docs/feedback/phase-3/*.png with docs/feedback/design_article.png, design_journal.png, design_serial.png and mocks 1e/1f end to end; confirm front-1920.png and article-1920.png are centred 1280 columns`.
**Depends on:** P5-04

## Spec issues
1. **Rule 34 amendment vs. `verify`.** "Phase 0 removes the four glob lines so the lint fails loudly until each phase lands" cannot coexist with `npm run lint` in `verify` after every task. Resolved by the tagged `# P<n>-<nn> pending` lines (Decision "Harness during the flight"); the same applies to `check-fixme.mjs`, which today fails on any `test.fixme(` but §6.9 asks Phase 0 to add ~190 of them. Suggest SPEC say "pending lines tagged with the owning task; empty at flight end".
2. **Rule 44 slug.** `article-h2` still generates `--wp--preset--font-size--article-h-2` (WordPress hyphenates every letter/digit boundary, exactly the `h2`→`h-2` bug). The plan keeps SPEC's slug and makes the checker compute generated names; a slug without a digit (e.g. `article-heading`) would avoid the hyphen entirely.
3. **"Series" in the nav on singles.** §6.1.1 says the current section on singles is the post's primary category; `Nav\CurrentSection` also marks `/series/` current for a post in a series (phase 1 P2-06), which would fail `nav-hub` on the seeded article. The plan removes that branch (Decision "Nav current section"); SPEC could state it.
4. **`core/post-terms` order.** SPEC expects "Technology · Security" from `core/post-terms`, but WordPress orders terms by name. Resolved by a `get_the_terms` filter ordering primary-first then nav order; SPEC could name the mechanism.
5. **Featured-image caption.** `core/post-featured-image` has no caption output; the plan injects a `figcaption` through a render filter (Decision "Featured-image caption"). Alternatively SPEC could accept a bound caption paragraph.
6. **`jr-count-hidden` counts DOM nodes.** A bound paragraph whose source returns '' still renders an empty `<p>`; the plan adds a generic "empty `ttm/*`-bound paragraph/heading renders nothing" filter. SPEC's 06 governing rule implies it; stating it would help.
7. **Colour rows below AA.** `js-words` (neutral-600 at 12px), `wr-chapter-num`/`ar-mostread-num`/`hub-part-num` (neutral-500 at 14–16px), `wr-serial-status` "Complete" (neutral-500), `nav-current` (accent) and the disabled pagination side (neutral-500) all fail axe `color-contrast` (serious), which the `a11y` row forbids. The plan asserts neutral-700/accent-700 as phase 2 did; SPEC §6.9 should carry the same override note.
8. **Hardening part 4 vs. the front-page lead.** §6.10 publishes part 4 without saying its category; a Technology post newer than part 3 would become the lead and break phase 2's front-page rows. The plan files part 4 under Security, dated today.
9. **Whole-row links with core blocks.** §6.4/§6.6 say the journal and archive rows are single links; core `post-title`/`post-excerpt` inside a Group cannot be wrapped in an `<a>` in block markup. Phase 1 shipped a linked title only. The plan converts the row Group into an anchor in a render filter (Decision "Whole-row links"); SPEC could name the mechanism or relax the row to "title is the link".
10. **`layout=list`/`rail` on `ttm/series-list`.** SPEC uses `layout=list` and `layout=rail`; the shipped enum is `rows|grid-2|grid-3|strip`. The plan renames `rows` → `list` and adds `rail` (rule 46 rename); SPEC §6.5/§6.6 could say so explicitly.
11. **Chapters heading text.** §6.5 wants "The Quiet Ledger — recent chapters" / "All 12" from `ttm/series-toc variant=chapters`, whose `heading` attribute today is a plain string. The plan composes the label in the block when `heading` is empty.
12. **"Stories" book form.** The mock's "Stories · 2019 · paperback" has no matching `Fiction\Books` form label; the plan renders `collection` as "Stories".
13. **`3b` series bar text.** The phone mock drops the word "Part" ("3 of 6"); markup cannot vary per viewport, so the phone keeps "Part 3 of 6" with the "Series" label and "View series" hidden.
14. **Inner masthead "one `<a>`".** Core `site-title` renders its own anchor; the "by Eric Mann" line is a sibling paragraph, not part of the same link.
15. **Nav close glyph.** With `hasIcon: false` the close button reads "Close", not "×" (01 §4.3); `hasIcon` governs both buttons.
16. **`§6.9 hub-progress-color`** assumes the featured series has both published and unpublished parts; the plan pins Hardening WordPress as `ttm_featured` in the seed so the auto-pick cannot drift to a completed series.
17. **Selectors row vs. block styles.** Rule 41 would flag every registered-but-unused `is-style-*` block style (04 §3 offers them in the inserter). The plan allow-lists them with the reason `editor block style (04 §3)`; SPEC could exempt `is-style-*` styles registered in `inc/block-styles.php` automatically.
18. **`wr-serial-form` regex** expects lowercase "monthly" while `wr-stat-cadence` expects "Monthly"; the plan capitalises only in the hero stat and leaves the term meta lowercase.
19. **§7 `extraVerify`** lists `themes/ttm-theme/theme.json` twice (as part of `themes/ttm-theme/` and on its own); harmless, kept as written.

## Review fixes (round 1)

### R1-01: Search result rows must be whole-row links
**Goal:** Make every row on /?s={q} a single anchor with the headline first in its text, as SPEC §6.6 "Search" requires ("rows as archive rows"), by rendering the matched-section kicker as bound plain text instead of core/post-terms and placing it after the title in the DOM.
**Files touched:** themes/ttm-theme/templates/search.html, plugins/ttm-core/src/Bindings/Sources.php, plugins/ttm-core/src/Bindings/Values.php, themes/ttm-theme/assets/css/ttm.css, tests/e2e/fidelity.spec.mjs, tests/integration/Theme/ArchiveTemplatesTest.php, tests/unit/Bindings/ValuesTest.php
**Design constraints:** Root cause: core/post-terms always renders a linked term, so the row content contains an `<a>` and `Blocks\Helpers::link_rows()` bails at its rule-33 nested-anchor guard (plugins/ttm-core/src/Blocks/Helpers.php:80). Do NOT weaken or special-case that guard — rule 33 (one anchor per row) still holds. Fix the template instead: replace `<!-- wp:post-terms {"term":"category","className":"is-style-kicker"} /-->` at search.html:34 with a `ttm/*`-bound paragraph carrying `class="is-style-kicker"` (extend `ttm/section-label` with a `search-row` format, or add an equivalent source, following PLAN Decision "New bindings this flight": pure formatting in Bindings\Values, a unit test for the normal and empty value, an integration test). Move the kicker AFTER `core/post-title` in the DOM so the accessible name starts with the headline (CLAUDE.md: "every whole-row link is one <a> with the headline first in its text"); `ttm.css`'s existing `.ttm-archive-row .is-style-kicker { grid-row: 1 }` already places it visually, so no new CSS should be needed — if the grid placement needs adjusting, keep it inside `/* 4.27 year group */`. The empty case (a result with no category) must render nothing, per `Bindings\Sources::drop_empty_bound()`. Rule 34: any class you add needs a `ttm.css` selector and vice versa. `themes/ttm-theme/assets/css/ttm.css` must stay within `cssBudgetBytes`; if R1-06 has already raised the budget, depend on it landing first.
**Acceptance tests:** New fidelity row `search-row-link` in tests/e2e/fidelity.spec.mjs (archive/search describe block): on SCREENS.search at 1280, assert `await page.locator('.ttm-archive-row').first().evaluate(n => n.tagName)` is `'A'` and that its `href` is non-empty — this is the assertion that would have caught the original finding, since today the element is a DIV. Also assert the anchor's `textContent` starts with the row title (headline-first), mirroring `ArticleTemplatesTest::test_journal_stream_rows_are_single_anchors_with_four_entries`. In tests/integration/Theme/ArchiveTemplatesTest.php, extend the search test to `assertMatchesRegularExpression('/<a href="[^"]+" class="wp-block-group ttm-archive-row[^"]*">\s*<h3 class="ttm-archive-row__title/', $html)` and `assertStringNotContainsString('<div class="wp-block-group ttm-archive-row', $html)`. Keep `search-row-kicker` green (12px / accent-700). New binding: unit tests in tests/unit/Bindings/ValuesTest.php for the normal and empty value (rule 26).
**Out of scope:** Changing `Helpers::link_rows()`'s nested-anchor guard or rule 33. Touching the category, tag, date or journal archive templates — I verified those already render `<a class="wp-block-group ttm-archive-row …">` correctly. Search ranking, pagination or the zero-results branch.
**Verification:** `curl -s 'http://localhost:8888/?s=ledger' | grep -o '<a href="[^"]*" class="wp-block-group ttm-archive-row[^"]*">' | head` returns matches and `grep -c '<div class="wp-block-group ttm-archive-row'` returns 0; `npm run test:e2e`; `npm run test:integration`; `npm run test:unit`; full `foundry_verify`.
**Depends on:** none

### R1-02: Rule 36: remove `layout: constrained` from inside grid groups
**Goal:** Satisfy SPEC §3.1 rule 36 (extended) — no `layout: constrained` group anywhere inside a `layout: default` grid group — and add the missing test that lets this class of violation ship.
**Files touched:** themes/ttm-theme/templates/page-writing.html, themes/ttm-theme/patterns/stat-row.php, tests/e2e/fidelity.spec.mjs
**Design constraints:** `themes/ttm-theme/templates/page-writing.html:41` (`ttm-writing-body__stories`) and `:59` (`ttm-writing-body__books`) are `"layout":{"type":"constrained"}` groups nested inside the `is-style-grid-7-5 ttm-writing-body` grid group at `:9`. Change both to `"layout":{"type":"default"}` in the block comment AND leave the matching `<div class="wp-block-group …">` markup consistent (core re-serialises the class list; verify the rendered class is `is-layout-flow`/`is-layout-constrained`-free). `themes/ttm-theme/patterns/stat-row.php:16,27,38` has the same shape against the core `layout: grid` group `.ttm-stats` — fix those three the same way in this pass. `ttm.css` already owns the width and gap of all of these, so no CSS should be needed; if a group visibly changes width, fix it with a `ttm.css` rule under its `01 §4` component header, not by restoring `constrained`. Watch the ≤1024 `display: contents` reorder at `ttm.css:3130-3133` — the whole point is that these become clean grid items there. Keep all existing `wr-*` fidelity rows green.
**Acceptance tests:** New fidelity row `wr-body-layout` in tests/e2e/fidelity.spec.mjs, modelled on `art-row-layout` (fidelity.spec.mjs:1089) but checking DESCENDANTS, not just direct children: on SCREENS.writing at 1280, iterate `.ttm-writing-body *` and assert no element's class list matches `/is-layout-constrained/`. This is the assertion that would have caught the original finding — `art-row-layout` only checks `.ttm-article > *` on the article screen, which is why these two groups survived the whole flight. Add the same descendant check for `.ttm-archive-body`, `.ttm-journal-head`, `.ttm-hub-head` and `.ttm-series-featured` so every `is-style-grid-*` screen is covered.
**Out of scope:** Top-level `layout: constrained` groups that are NOT inside an `is-style-grid-*` group (`index.html:3`, `front-page.html:3`, `page-series.html:33`, `parts/header-front.html:1`, `patterns/code-figure.php:14`, `patterns/masthead-front.php:64`) — those are permitted by rule 36 as written. Any visual redesign of the Writing aside.
**Verification:** `grep -n 'constrained' themes/ttm-theme/templates/page-writing.html themes/ttm-theme/patterns/stat-row.php` returns nothing; re-run the nesting sweep over `themes/ttm-theme/{templates,parts,patterns}` and confirm zero `constrained`-inside-`is-style-grid-*` hits; `npm run test:e2e` (all `wr-*` rows plus the new layout rows); full `foundry_verify`.
**Depends on:** none

### R1-03: Fix the dead ≤720 override on the series featured part rows
**Goal:** Make SPEC §6.7's phone rule actually apply — "≤ 720: … part rows `28px 1fr` with the date on a second line" — by moving the media-query block after the base rule it must beat, and add the assertion that would have caught it.
**Files touched:** themes/ttm-theme/assets/css/ttm.css, tests/e2e/fidelity.spec.mjs
**Design constraints:** `themes/ttm-theme/assets/css/ttm.css:2697` declares `.ttm-series-featured__part { grid-template-columns: 28px 1fr }` inside `@media (max-width: 720px)` (block opens at :2687), but the unscoped base rule `.ttm-series-featured__part { display: grid; grid-template-columns: 40px 1fr auto; … }` sits LATER at `:3012`. Specificity is equal (0,1,0) and a media query adds none, so source order wins and the phone override is dead at every viewport. Move the whole `@media (max-width: 720px)` block at `:2687-2704` (it also carries `.ttm-hub-head`, `.ttm-hub-head.is-style-grid-8-4 h1` and `.ttm-series-featured__date`) to immediately after `.ttm-series-featured__date`'s base rule (ends `:3051`) — do not duplicate or raise specificity to win, since P5-01 already recorded source-order-vs-cascade as the general hazard. While there, re-scan the whole file for the same pattern (a `@media` rule for a selector that also has a later unscoped same-specificity rule) and fix any other hit the same way; this scan is the substance of the task, not an extra. Keep `hub-phone`, `hub-part`, `hub-part-num`, `hub-part-scheduled` and `single-parts` green. Respect `cssBudgetBytes` (the move should be byte-neutral).
**Acceptance tests:** Extend `hub-phone` (tests/e2e/fidelity.spec.mjs:2707) with `expect( ( await tracks( page.locator( '.ttm-series-featured__part' ).first() ) ).length ).toBe( 2 )` at 390 — today that returns 3 tracks, so this assertion fails against the current code and would have caught the finding. Add the mirror assertion for `/series/hardening-wordpress/` (`.ttm-series-single .ttm-series-featured__part`) at 390, and assert `.ttm-series-featured__date` computes `grid-column-start: 2` there so the "date on a second line" half of the SPEC rule is covered too.
**Out of scope:** Redesigning the part row at desktop. Changing `hub-part`'s 1280 assertions. The zero-padding of `.ttm-series-featured__num` — that is an open owner decision recorded in docs/REVIEW.md Spec issue 5.
**Verification:** Confirm by reading the file that no `@media` rule for a `ttm-` selector is followed by a later unscoped same-specificity rule for the same selector; `npm run test:e2e`; `npm run screenshots` and eyeball `series-hub.png`/`series-single.png` are unchanged at 1280; full `foundry_verify`.
**Depends on:** none

### R1-04: Archive row dates and the serial meta line must match SPEC's literal text
**Goal:** Stop the year-grouped archive rows repeating the year that their own group label already shows (SPEC §6.6: date `("Sept 10")` in a `72px 1fr` grid), and lower-case the cadence in the serial list meta line per SPEC §6.5.
**Files touched:** plugins/ttm-core/src/Bindings/Sources.php, plugins/ttm-core/src/Bindings/Values.php, plugins/ttm-core/src/Support/Dates.php, themes/ttm-theme/templates/category.html, themes/ttm-theme/templates/archive.html, plugins/ttm-core/blocks/series-list/render.php, tests/e2e/fidelity.spec.mjs, tests/unit/Bindings/ValuesTest.php, tests/integration/Blocks/SeriesListTest.php, tests/integration/Theme/ArchiveTemplatesTest.php
**Design constraints:** (1) `ttm/short-date` resolves to `Support\Dates::short()` (plugins/ttm-core/src/Support/Dates.php:36-44), which appends `, {Y}` when the year differs from `Clock::now()`. Inside `ttm/archive-by-year`'s year groups the year is already the 120px label, so `/category/security/` renders "Nov 26, 2025" in a 72px column, wraps to two lines and knocks the title off the group baseline — visible in docs/feedback/phase-3/archive-security.png. Add an argument to the `ttm/short-date` binding source (e.g. `{"noYear":true}`) and use it on the `ttm-archive-row__date` paragraphs in `category.html:19` and `archive.html:17`. Do NOT change `Dates::short()`'s own behaviour — the journal stream (§6.4), the search rows and the hub part dates are not year-grouped and must keep the year. Put the year-less formatting in `Bindings\Values` as a pure formatter (PLAN Decision "New bindings this flight"). (2) `plugins/ttm-core/blocks/series-list/render.php:174-177` capitalises cadence in the `list`/`grid-*` `__meta` line; SPEC §6.5 writes it lowercase ("Novel · literary thriller · monthly") and PLAN spec-issue #18 says capitalisation belongs only to `ttm/serial-hero`'s stat value. Leave the hero stat capitalised (SPEC §6.9 `wr-stat-cadence` requires "Monthly"). Rule 24: no new bare numeric literals. Text stays translatable.
**Acceptance tests:** Extend `ar-row-date` (tests/e2e/fidelity.spec.mjs) with a text assertion on a row in the SECOND year group (the 2025 group on /category/security/): `expect( txt.trim() ).toMatch( /^[A-Z][a-z]{2,4} \d{1,2}$/ )` — today that row reads "Nov 26, 2025" and this fails, which is the assertion that would have caught the finding. In tests/integration/Theme/ArchiveTemplatesTest.php add a case that seeds a post dated in a prior year and asserts the rendered `ttm-archive-row__date` contains no 4-digit year, while a journal-stream row for the same post still does. Unit tests in tests/unit/Bindings/ValuesTest.php for the new arg: normal value, prior-year value, empty value (rule 26). For the cadence: change `SeriesListTest.php:289` to assert `'ttm-series-row__meta">Novel · literary thriller · monthly<'` and add `assertStringNotContainsString('· Monthly<', $html)`; keep `SerialHeroTest`'s `'<span class="ttm-stats__value">Monthly</span>'` assertion untouched.
**Out of scope:** Redesigning the year-group layout or widening the 72px date column. Changing the journal stream, search row or hub part date formats. `Dates::short()`'s signature or its existing callers.
**Verification:** `curl -s http://localhost:8888/category/security/ | grep -o 'ttm-archive-row__date[^<]*<[^>]*>[^<]*' | tail -3` shows no 4-digit years; `npm run screenshots` and confirm the 2025 group's dates no longer wrap in archive-security.png; `npm run test:e2e`; `npm run test:integration`; full `foundry_verify`.
**Depends on:** none

### R1-05: Close the test gaps on the flight's own late fixes
**Goal:** Make the four fixes that were made late in the flight — the wpautop `<p>` fix, the archive-aside label case, the most-read numbering, and the grid-2 category join — fail loudly if they are reverted. Every one of these is green today with the mechanic removed.
**Files touched:** tests/e2e/fidelity.spec.mjs, tests/integration/Blocks/MostReadTest.php, tests/integration/Blocks/SeriesListTest.php
**Design constraints:** No production code changes — all four mechanics are already correct and I verified them against the running seeded site and the committed screenshots. This task adds assertions only. (1) `wr-synopsis` (fidelity.spec.mjs:1746) and `hub-dek` (:2550) assert only font-size/colour/max-width, so deleting the `'raw'` argument from `serial-hero/render.php:47` or `series-featured/render.php:134` leaves both green; SPEC §6.9's rows specify only style properties so there is nothing to tighten — the assertion must be added. (2) `ar-aside-series` (:2282) asserts `textContent`, which `text-transform` does not affect, so re-adding P3-04's `.ttm-archive-body aside .ttm-cell-heading__label { text-transform: none }` override would be green; sibling rows using the same `textContent` idiom (`cell-head` :455, `writing-head` :629, :1107, :1288) all PAIR it with a computed `text-transform` assertion — match that pattern. Mock 1e confirms uppercase: both headings are `<h6>` and the mock's design system sets `h6 { letter-spacing: .08em; text-transform: uppercase }`. (3) `tests/integration/Blocks/MostReadTest.php` never asserts the number text, so reverting `most-read/render.php:75` to `sprintf( '%02d', … )` passes everything; mock 1e shows literal `1`/`2`/`3`. (4) `SeriesListTest.php:266` asserts a single category, so nothing covers the `grid-2` " · " join; `hub-grid-cats` (:2698) is `expect( t.includes(' · ') || t.length > 0 ).toBe( true )`, whose second disjunct makes the first unreachable.
**Acceptance tests:** (1) Add to both `wr-synopsis` and `hub-dek`: `expect( await el.innerHTML() ).not.toContain( '<p>' )` and `expect( await el.innerHTML() ).not.toContain( '&lt;p&gt;' )`. (2) Add to `ar-aside-series`: `expect( await computed( el, 'text-transform' ) ).toBe( 'uppercase' )`, and add the same pair (textContent 'Most read' + uppercase) as a new row `ar-mostread-head` for `.ttm-most-read .ttm-cell-heading__label`. (3) In MostReadTest: `assertStringContainsString( '<span class="ttm-numbered__num tnum">1</span>', $html )` and `assertStringNotContainsString( '>01<', $html )`. (4) In SeriesListTest's grid-2 test, seed a second category on the series and assert `'ttm-series-row__categories">Technology · Security<'`; drop the unreachable disjunct in `hub-grid-cats` so it asserts the real shape. Prove each new assertion by temporarily reverting the corresponding mechanic and confirming the test fails, then restoring.
**Out of scope:** Changing any of the four mechanics — they are all correct as shipped. Adding a `docs/PROGRESS.md` entry for dd4dfbe (that belongs to R1-10). SPEC §6.9 wording (that is a recorded spec issue).
**Verification:** For each of the four: revert the mechanic, run the owning suite, confirm a failure naming the new assertion, restore with `git checkout --`, re-run green. Then `npm run test:e2e`, `npm run test:integration`, full `foundry_verify`, and confirm `git status` is clean.
**Depends on:** none

### R1-06: Raise cssBudgetBytes and restore the declarations dropped under it
**Goal:** Give ttm.css real headroom (it ships at 61439 of 61440 bytes — one byte) and put back every declaration SPEC/PLAN named that was dropped to fit, each guarded by a fidelity row so it cannot silently vanish again.
**Files touched:** scripts/check-budget.mjs, CLAUDE.md, themes/ttm-theme/assets/css/ttm.css, tests/e2e/fidelity.spec.mjs
**Design constraints:** Set `cssBudgetBytes = 62464` (the next 1024 multiple above the current size) in `scripts/check-budget.mjs` and in CLAUDE.md's Constraints line — SPEC §3.1 rule 30 says the number lives ONLY in those two places, still marked ⚠️ ASSUMPTION. Then restore, each under its `01 §4` component comment header, no hex literals, presets via `var(--wp--preset--…)`: `.ttm-serial-hero` grid `280px minmax(0,1fr)` (ttm.css:3059 — this is what forced P5-01's `.ttm-serial-hero__body { min-width: 0 }` band-aid; keep the min-width too, belt and braces); `__kicker` `margin: 0 0 12px` (:3067 currently `margin-bottom: 10px`, the wrong value); `__title` `margin: 0 0 16px -0.04em` (:3071, absent); `__synopsis` `line-height: 1.45; margin: 0 0 20px` (:3075, both absent); `__buttons` `gap: 10px; margin: 0 0 22px` (:3085 currently 12px/12px both sides); a `.ttm-stats__value { font-size: 16px }` rule inside the ≤720 block (:3159-3186, absent entirely — SPEC §6.5 "stats 3-across at 16px values", and NOT in HANDOFF's disclosure list); chapters `__dek` 13px neutral-800 margin-top 3 (:2806) and `__date` 12px neutral-700 tnum (:2816) and `__title` line-height 1.2 (:2811) and row `align-items: baseline` (:2796), all currently inheriting `.ttm-numbered__row`'s 14px/600/1.35; list-row mark margin-top 6 / dek margin-top 4 / meta margin-top 6 (:1613, :1717, :1684 — currently 5/5/4); `__count` colour neutral-700 and `font-feature-settings` tnum (:1745, both absent). Un-condense the comments that were compressed past legibility (ttm.css:46, :512, :937, :979, :2283). PLAN's Decision "Colour vs a11y" still applies: any restored colour on text under 18.66px uses accent-700/neutral-700, never accent/neutral-500/neutral-600 — the `a11y` row must stay at 0 serious/critical. Rule 41: every restored selector must still match at least one element on the seeded screen set.
**Acceptance tests:** A fidelity row per restored value, since the absence of one is exactly why each was droppable: extend `wr-hero` with `expect(t[0]).toBeCloseTo(320,0)` and assert the second track shrinks below its intrinsic width; extend `wr-synopsis` with `line-height` 27.55px and `margin-bottom` 20px; extend `wr-kicker` with `margin-bottom` 12px; add `wr-title-margin`; extend `wr-buttons` with `column-gap` 10px and the container's `margin-bottom` 22px; add `wr-stat-value-phone` asserting `.ttm-stats__value` font-size 16px at 390; extend `wr-chapter-title` with `line-height` 20.4px and add `wr-chapter-dek` (13px / neutral-800 / margin-top 3px) and `wr-chapter-date` (12px / neutral-700); extend `wr-serial-count` with `color` neutral-700. Each new assertion must fail against the current CSS before the restoration — check that first, so the test is proven to bite.
**Out of scope:** Adding CSS for anything SPEC/PLAN did not name. Restoring the `.ttm-archive, .ttm-most-read { display: block }` no-op or the dead `.ttm-footer__copyright:empty` rule — those are deletions owned by R1-10. Any change to theme.json presets.
**Verification:** `node scripts/check-budget.mjs` prints the new size against 62464 with visible headroom; `grep -rn '61440' scripts/ CLAUDE.md docs/` returns nothing stale; `npm run test:e2e` (every `wr-*`, `hub-*` and `a11y` row green, new rows passing); `npm run lint`; full `foundry_verify`.
**Depends on:** none

### R1-07: Seed fixture drift: the third book, and a tautological Sunday test
**Goal:** Bring docs/fixtures/seed/books.json back to SPEC §6.10's two books, and make the journal-Sunday test actually exercise the weekday walk-back it is the only coverage for.
**Files touched:** docs/fixtures/seed/books.json, tests/integration/Cli/SeederTest.php
**Design constraints:** (1) `docs/fixtures/seed/books.json` holds three books — `The Quiet Ledger`, `Salt Water Wires`, `Eleven Small Doors`. SPEC §6.10's Writing bullet and PLAN P0-08 both name exactly two, and mock 2d's "In print" grid (docs/Eric Mann Newspaper.dc.html:520-524) shows those two; the Writing page currently renders a book the mock does not have. Remove the `The Quiet Ledger` row. The Seeder reads books.json as add/update rather than replace, so also confirm a `wp ttm seed --reset` run does not leave a stale third book behind — if it does, that is part of this fix. (2) `tests/integration/Cli/SeederTest.php:409-422` (`test_journal_post_one_is_on_a_sunday_with_location_and_syndication`) sets now to `2026-09-20`, which IS a Sunday, and `journal-post-1` carries `"days_ago": 0` — so the post date equals now and is a Sunday whatever the code does. The walk-back loop at `plugins/ttm-core/src/Cli/Seeder.php:366-369` could be deleted and the test stays green. Do not change the fixture's `weekday`/`days_ago` values or the Seeder logic; change the test's reference date. Rule 39: no lorem, original prose only. Keep every `wr-books`/`wr-book-*` fidelity row green (they assert a 2-equal-track grid, so two books is what they expect).
**Acceptance tests:** (1) Replace the per-title `assertContains()` calls at SeederTest.php:531-549 with an exact set assertion — `assertSame( [ 'Eleven Small Doors', 'Salt Water Wires' ], $sorted_titles )` — which is what would have caught the extra row; the current `assertContains` form cannot. (2) Change `set_now( '2026-09-20 12:00:00' )` to a Wednesday (`2026-09-23 12:00:00`) and, in addition to the existing `assertSame( 'Sunday', gmdate( 'l', … ) )`, assert the date moved back exactly 3 days from `now` — so deleting the walk-back loop fails the test. Prove it: delete the loop, confirm failure, restore.
**Out of scope:** Any other seed fixture (posts.json, prose.json, series). The Seeder's image generation or `seed.image_band_angle`. The Writing page's book-grid CSS or layout.
**Verification:** `npx wp-env run cli wp ttm seed --reset` then `curl -s http://localhost:8888/writing/ | grep -c 'ttm-book__title'` returns 2; `grep -ri lorem docs/fixtures/seed/` returns nothing; `npm run test:integration`; `npm run test:e2e`; `npm run screenshots` and confirm writing.png's "In print" grid matches mock 2d; full `foundry_verify`.
**Depends on:** none

### R1-08: Scope the global post-excerpt filter; newsletter box copy is 13px
**Goal:** Stop `Helpers::excerpt_markup` from rewriting every excerpt site-wide, and correct the newsletter box copy size to SPEC §6.2's 13px.
**Files touched:** plugins/ttm-core/src/Blocks/Helpers.php, themes/ttm-theme/assets/css/ttm.css, tests/unit/Blocks/HelpersTest.php, tests/e2e/fidelity.spec.mjs
**Design constraints:** (1) `plugins/ttm-core/src/Blocks/Helpers.php:45` registers `excerpt_markup` on `render_block_core/post-excerpt` with no guard at all, so for any post whose manual excerpt contains `<` it replaces core's `wp_trim_words()` output with the full kses'd excerpt — on the front-page lead dek, "More in" rows, archive rows and journal stream rows, not just the article header it was added for (P1-03). Its sibling `featured_caption` (`:43`) correctly guards on `is_singular()`. Scope it the same way: return `$block_content` unchanged unless `is_singular()`, or key off `$block['attrs']['className']` containing `is-style-dek-l` — whichever keeps `art-dek` and `art-dek-code` green, and prefer the className form if it is sufficient, since it is narrower. Keep the existing kses allow-list (`code`, `em`, `strong`) exactly as is. (2) `themes/ttm-theme/assets/css/ttm.css:991-995` sets `.ttm-newsletter-box__copy { font-size: var(--wp--preset--font-size--body-s) }` = 14px; SPEC §6.2 "Newsletter box" and PLAN P1-06 both say 13px, i.e. the `ui` preset. Change it to `var(--wp--preset--font-size--ui)`. Rule 44: reference the preset by slug, no literal px. Respect `cssBudgetBytes` (byte-neutral change).
**Acceptance tests:** (1) In tests/unit/Blocks/HelpersTest.php add a case asserting `excerpt_markup()` is a NO-OP for a block whose className lacks `is-style-dek-l` (or, if the `is_singular()` form is chosen, with `Functions\when('is_singular')->justReturn(false)`), given a post with markup in its manual excerpt — the existing tests only cover the positive path, which is why the missing guard shipped. Keep the existing positive-path assertions green. (2) New fidelity row `box-copy` in tests/e2e/fidelity.spec.mjs: on SCREENS.article at 1280, `.ttm-newsletter-box__copy` font-size is `px(13)` and colour `neutral-800` — this fails against the current 14px, which is the assertion that would have caught the finding. SPEC §6.9 has no such row; add it and note the addition in the commit body.
**Out of scope:** `Newsletter\Form::render()` or the newsletter provider dispatch (§6.3 is unchanged this flight). The other `render_block_core/*` filters in Helpers.php. Splitting Helpers.php into a CoreFilters class — that is a refactor, not a fix.
**Verification:** `curl -s http://localhost:8888/ | grep -o 'wp-block-post-excerpt__excerpt">[^<]*'` shows the front-page deks are still core-trimmed; `npm run test:unit`, `composer test:unit`, `npm run test:e2e` (`art-dek`, `art-dek-code`, `lead-dek`, `cell-lead-dek`, `box-*` and the new `box-copy` all green); full `foundry_verify`.
**Depends on:** none

### R1-09: Tighten the fidelity rows that assert less than their §6.9 row
**Goal:** Make nine fidelity rows actually assert what SPEC §6.9's `expected` column says, so a real regression in each would fail. All nine are green today with the mechanic broken.
**Files touched:** tests/e2e/fidelity.spec.mjs
**Design constraints:** Test-only task; no production code changes. `tests/e2e/lib/style.mjs`'s `tracks()` already returns resolved pixel numbers and the suite asserts them elsewhere (`fidelity.spec.mjs:1018` does `expect( t[0] ).toBeCloseTo( 10, 0 )`), so the track-size looseness is avoidable, not a harness limitation. Tolerance stays ±0.5px (`toBeCloseTo( n, 0 )`) per PLAN Decision "Fidelity mechanics". Rows to fix: `art-row` (:1080) asserts `t.length === 2` where §6.9 says `2:1 tracks` — add `expect( t[0]/t[1] ).toBeCloseTo( 2, 1 )`; `art-hero` (:1194) evaluates `filter: none` on the FIGURE while every grayscale rule in ttm.css (:347, :1182, :1481) targets an `img`, so assert on `… .wp-block-post-featured-image img`; `art-byline-author` (:1175) drops §6.9's `color: text` half — add it; `art-byline-tags` (:1183) drops §6.9's "bounding box: right of the read-time span" — compare the chip's `boundingBox().x` against the read-time span's right edge, per PLAN Decision "Fidelity mechanics: margin-left: auto asserted by position"; `toc-item` (:1329) — add `expect( t[0] ).toBeCloseTo( 28, 0 )`; `aside-phone-order` (:1441) starts `lastY` at prev/next's TOP so an overlapping zone passes — use `prevnext.y + prevnext.height`; `ar-year` (:2140), `ar-row`, `ar-mostread`, `ar-aside-row` (:2296) assert track count only — add `toBeCloseTo` on the fixed track (120, 72, 24, 10). Also add a phone assertion that `.ttm-series-single h1` computes 44px at 390, which is the only thing P5-01's media-query-order fix is currently missing (it is covered only indirectly, by the overflow loop).
**Acceptance tests:** The tightened rows ARE the tests. Prove each one bites before considering it done: for every row, temporarily break the mechanic it now covers (e.g. change `is-style-grid-8-4` to `-6-6` for `art-row`; add `filter: grayscale(1)` to the hero img for `art-hero`; change `120px 1fr` to `200px 1fr` for `ar-year`), confirm the row fails, then restore with `git checkout --`. Record in the commit body which break was used for each row.
**Out of scope:** Changing production CSS, markup or PHP to make a tightened row pass — if a row fails after tightening, that is a new finding to report, not something to paper over. SPEC §6.9's own loose wording for `hub-grid-cats` and `wr-serial-form` (recorded as spec issues; `hub-grid-cats` is owned by R1-05).
**Verification:** `npm run test:e2e` fully green after all restorations; `git status` clean; full `foundry_verify`.
**Depends on:** none

### R1-10: Prefix, stale lint entries, no-op CSS and the undocumented dd4dfbe commit
**Goal:** Clear the hygiene debt that the coverage and selector lints were satisfied around rather than by: restore the mandated `ttm-` prefix, delete two dead rules and two false allow-list reasons, and record the out-of-sequence commit in PROGRESS.
**Files touched:** plugins/ttm-core/src/Fiction/Books.php, scripts/check-css-coverage.mjs, scripts/test/check-css-coverage.test.js, themes/ttm-theme/assets/css/ttm.css, tests/e2e/selectors-allow.txt, docs/PROGRESS.md
**Design constraints:** (1) `plugins/ttm-core/src/Fiction/Books.php:128` emits `class="book-admin-row"`, renamed from `ttm-book-row` purely to silence `scripts/check-css-coverage.mjs`. CLAUDE.md: "Prefixes: … CSS `ttm-`". Restore `ttm-book-row` and instead add `src/Fiction/Books.php` to the scanner's existing `SRC_SKIP` list (`scripts/check-css-coverage.mjs:36`, currently `[]`, already documented as "files known not to contain front-end markup") — this is wp-admin-only markup that `ttm.css` correctly does not style. (2) Delete `themes/ttm-theme/assets/css/ttm.css:2426-2429` `.ttm-archive, .ttm-most-read { display: block }`, a self-admitted no-op P3-06 added to clear rule 34's letter; if the coverage lint then reports those two wrapper classes, resolve it the same way — via the scanner's wrapper handling, not a decorative rule. (3) Delete `ttm.css:1119` `.ttm-footer__copyright:empty` and its `selectors-allow.txt` line: `Bindings\Sources::drop_empty_bound()` removes empty `ttm/*`-bound paragraphs entirely, so the selector can never match and its stated reason is false. (4) Delete the `selectors-allow.txt` line `.ttm-series-featured__part-dek # state: only when the block's showDek attribute is true; the hub template leaves it at its false default` — false since P4-03: `themes/ttm-theme/templates/taxonomy-series.html:5` sets `"showDek":true`, `/series/hardening-wordpress/` is in `SCREEN_URLS`, and `single-parts` asserts `deks.count() > 0`. The entry exempts a LIVE selector from dead-selector detection, which is the opposite of rule 41's purpose. (5) Add a `docs/PROGRESS.md` log entry for `dd4dfbe` under the phase it corrects, and fix `docs/PROGRESS.md:158`, which still documents P3-04's `text-transform: none` override as shipped although dd4dfbe removed it. Do not edit `docs/phase-1/` or `docs/phase-2/`. `scripts/css-coverage-allow.txt` must stay 0 bytes and `ALLOW_PENDING`/`ALLOW_TAGGED` stay false.
**Acceptance tests:** Extend `scripts/test/check-css-coverage.test.js` with a case asserting that a class emitted only from a `SRC_SKIP`-listed file is not reported as missing, and that a class emitted from a non-skipped file still is — this is what lets the prefix be restored without the lint forcing a rename, and it is the test that would have prevented the original workaround. For (3) and (4), the existing `tests/e2e/selectors.spec.mjs` is the test: after deleting the two lines it must still pass, proving `.ttm-series-featured__part-dek` really does match and that the deleted `.ttm-footer__copyright:empty` rule is gone rather than merely exempted.
**Out of scope:** Rewriting the coverage scanner's architecture. The `# editor block style (04 §3)` and remaining `# state: …` entries in selectors-allow.txt — those are a recorded spec issue (ratifying the `state:` category) for the owner, not a code fix. Any other PROGRESS.md log entry.
**Verification:** `grep -rn 'book-admin-row' plugins/` returns nothing and `grep -rn 'ttm-book-row' plugins/` matches; `node scripts/check-css-coverage.mjs` reports 0 missing, 0 dead, 0 pending with `scripts/css-coverage-allow.txt` still 0 bytes; `npm run test:e2e` (selectors spec green); `npm run test:unit`; full `foundry_verify`; `git diff --stat` shows no `docs/phase-1/` or `docs/phase-2/` change and no `FOUNDRY_FEEDBACK.md` change.
**Depends on:** none

## Review fixes (round 2)

### R2-01: Scope R1-06's series-row margins to the list layout; restore strip, rail and grid-2 values
**Goal:** Undo the regression R1-06 introduced by changing shared base rules. Restore the front-page/404 strip (mark 5px, meta 4px, phase-2 SPEC §6.1.7), the category rail (mark 5px, PLAN P3-04) and the hub grid-2 dek (5px, SPEC §6.7). Keep the Writing list layout at mark 6 / dek 4 / meta 6 (PLAN P4-05). Add the fidelity rows R1-06's acceptance criterion required but did not add.
**Files touched:** themes/ttm-theme/assets/css/ttm.css, tests/e2e/fidelity.spec.mjs
**Design constraints:** In themes/ttm-theme/assets/css/ttm.css under /* 4.16 series mark */ and /* 4.17 series row */, restore the base values that shipped before R1-06: `.ttm-series-mark { margin-top: 5px }` (currently 6px, ~:1615); `.ttm-series-row__meta { margin-top: var(--wp--preset--spacing--10) }` (currently 6px, ~:1684); `.ttm-series-row__dek { margin-top: 5px }` (currently 4px, ~:1717). Then add list-only overrides placed AFTER those base rules in source order (the P5-01/R1-03 source-order hazard): `.ttm-series-list.is-list .ttm-series-mark { margin-top: 6px }`, `.ttm-series-list.is-list .ttm-series-row__meta { margin-top: 6px }`, `.ttm-series-list.is-list .ttm-series-row__dek { margin-top: 4px }` (the last can join the existing `.ttm-series-list.is-list .ttm-series-row__dek { max-width: 46ch }` rule). Leave the existing `.ttm-series-list.is-grid-2 .ttm-series-mark { margin-top: 6px }` and `.ttm-series-list.is-rail .ttm-series-row__meta { margin-top: 3px }` overrides as they are. Presets via var(--wp--preset--…), no hex (rule 44). Stay within cssBudgetBytes 62464 (rule 30); do not raise it. Rule 41: every new selector must match on the seeded screen set (/writing/ renders `.is-list`). SPEC §2 non-goal: no other front-page change.
**Acceptance tests:** These assertions would have caught the regression, and each must fail against the current CSS before the fix. In tests/e2e/fidelity.spec.mjs: extend `strip-mark` (front, 1280) with `margin-top` px(5); extend `strip-meta` with `margin-top` px(4); extend `ar-aside-row` with the rail mark's `margin-top` px(5); add `hub-grid-dek` (/series/, 1280) asserting `.ttm-series-list.is-grid-2 .ttm-series-row__dek` (first) `margin-top` px(5); add `wr-serial-row-margins` (/writing/, 1280) asserting, in `.ttm-series-list.is-list`, mark `margin-top` px(6), `.ttm-series-row__dek` px(4) and `.ttm-series-row__meta` px(6). Before the CSS change, run the new and extended rows and confirm strip-mark, strip-meta, ar-aside-row and hub-grid-dek fail. After it, confirm all pass.
**Out of scope:** Any other series-row property. The grid-2 categories line, the count/status cell, or the strip's grid. Changing cssBudgetBytes. Screenshots (R2-02).
**Verification:** A Playwright computed-style probe at 1280 on the running seeded site shows `/` strip mark 5px and meta 4px, `/category/security/` rail mark 5px, `/series/` grid-2 dek 5px, and `/writing/` list mark/dek/meta 6/4/6px. `npm run test:e2e` is green, including every phase-2 front-page row. `npm run lint` passes (budget and coverage). Full foundry_verify.
**Depends on:** none

### R2-02: Regenerate and commit the phase-3 screenshots after the round-1 fixes
**Goal:** Make docs/feedback/phase-3/*.png show the final code, as SPEC §1 'Done' and §8 require. The committed set dates from P5-05 (ebf010f) and predates R1-01, R1-04, R1-06, R1-07, R1-08 and R2-01, so the owner's manual mock comparisons would validate superseded output.
**Files touched:** docs/feedback/phase-3/404.png, docs/feedback/phase-3/archive-390.png, docs/feedback/phase-3/archive-security.png, docs/feedback/phase-3/article-1920.png, docs/feedback/phase-3/article-390.png, docs/feedback/phase-3/article.png, docs/feedback/phase-3/front-1920.png, docs/feedback/phase-3/journal-390.png, docs/feedback/phase-3/journal.png, docs/feedback/phase-3/search.png, docs/feedback/phase-3/series-hub.png, docs/feedback/phase-3/series-single.png, docs/feedback/phase-3/writing-390.png, docs/feedback/phase-3/writing.png, docs/HANDOFF.md
**Design constraints:** Run `npm run env:cli -- ttm seed --reset` (or `npm run test:e2e`, which reseeds), then `npm run screenshots`. Commit all 14 PNGs listed in SPEC §6.11 in one commit. Do not change code or CSS in this task. Update docs/HANDOFF.md's round-1 'What a human should check by hand' item 4 to say the screenshots are now current, and name the commit. Never stage FOUNDRY_FEEDBACK.md. If any test switches the active theme, restore ttm-theme afterwards.
**Acceptance tests:** This check would have caught the finding: the newest commit touching docs/feedback/phase-3/ must be newer than the newest commit touching themes/ttm-theme/, plugins/ttm-core/blocks/ or docs/fixtures/seed/, i.e. `git merge-base --is-ancestor $(git log -1 --format=%H -- themes/ttm-theme plugins/ttm-core/blocks docs/fixtures/seed) $(git log -1 --format=%H -- docs/feedback/phase-3)` exits 0. Before this task it exits 1. Record it in the commit body. Also confirm by eye: writing.png shows exactly two books in 'In print'; archive-security.png's 2025 group dates read 'Nov 26' on one line; search.png rows show the plain-text kicker; the front-page strip in front-1920.png is unchanged from phase 2.
**Out of scope:** Any visual fix found while looking at the PNGs. Record it in HANDOFF as a manual-check note for the owner, not a code change. The phase-2 screenshot directory.
**Verification:** The git ancestry check above exits 0. `git show --stat HEAD` lists the 14 PNGs and HANDOFF.md only. Full foundry_verify.
**Depends on:** R2-01

## Review fixes (round 3)

### R3-01: Restore the SPEC §6.2/§6.4/§6.5/§6.7 values no fidelity row asserted
**Goal:** Make twelve computed values on the article, journal, hub and Writing screens match SPEC prose and the mock cards. They drifted because no §6.9 row read them (REVIEW round 2, finding 3). The most visible one: the paragraph after the article's code block sits flush against it.
**Files touched:** themes/ttm-theme/assets/css/ttm.css, tests/e2e/fidelity.spec.mjs, scripts/check-budget.mjs, CLAUDE.md
**Design constraints:** All edits are CSS in themes/ttm-theme/assets/css/ttm.css, under the existing 01 §4 component headers. Presets via var(--wp--preset--…), no hex (rule 44).

(a) `.entry-content pre`: margin-bottom 22px (mock 2b `<pre style="margin:0 0 22px">`). Keep the ≤720 full-bleed negative inline margins at ~:2316; set margin-bottom only, not the `margin` shorthand.
(b) `.wp-block-quote.is-style-pull, blockquote.is-style-pull` (~:611): margin 36px 0 (SPEC §6.2 'margins 36'; spacing--60 is 32px) and letter-spacing -0.015em.
(c) `.ttm-journal-stream .ttm-cell-heading__link` and `.ttm-hub-all .ttm-cell-heading__link`: font-size var(--wp--preset--font-size--caption) (12px), color neutral-700 (SPEC §6.4, §6.7). Do NOT change the base `.ttm-cell-heading__link` (~:926). Its 11px is phase 2's front-page value, and SPEC §2 forbids moving any front-page row.
(d) `.ttm-series-progress__meta` (~:2716): margin 0 0 18px (SPEC §6.7).
(e) `.ttm-series-featured__buttons` (~:2996): gap 10px (SPEC §6.7, mock 1f `gap:10px`). Keep its other margins unless SPEC says otherwise.
(f) `.ttm-stats__label` (~:2939): 13px (SPEC §6.5 stat row '13px neutral-700', mock 2d `font-size:13px`). No front-page element carries .ttm-stats.
(g) `.ttm-story-tiles` and `.ttm-book-grid`: padding-top 16px (SPEC §6.5).
(h) `.ttm-book__meta`: margin-top 2px (SPEC §6.5; currently 12px, core p margin).
(i) `.ttm-series-toc.is-chapters .ttm-numbered__dek, .ttm-series-toc.is-chapters .ttm-numbered__date`: font-weight 400 (mock 2d rows set no weight). Scope to `.is-chapters`: `.ttm-numbered__date` is shared with the 404 Latest list and most-read.

Place every override after the base rule it must beat, in source order and outside any media query that would shadow it (the P5-01/R1-03 hazard).

Rule 41: every new selector must match on the seeded screen set.

Rule 30 (budget): ttm.css is 62428/62464. First shorten comments losslessly. If the additions still do not fit, raise cssBudgetBytes to the next 1024 multiple (63488) in scripts/check-budget.mjs (with its comment) and in CLAUDE.md's Constraints line, and add a Measurement: line with before/after bytes. Never drop a declaration to fit.
**Acceptance tests:** Each extended assertion must fail against the current CSS before the fix and pass after it. Record the failures in the commit body. In tests/e2e/fidelity.spec.mjs:
- `art-pre`: margin-bottom px(22).
- `art-pull`: margin-top and margin-bottom px(36); letter-spacing ≈ −0.42px (toBeCloseTo on parseFloat).
- `js-link`: font-size px(12), color neutral-700.
- `hub-all-head`: font-size px(12).
- `hub-meta`: margin-top px(0), margin-bottom px(18).
- `hub-buttons`: `.ttm-series-featured__buttons` column-gap px(10).
- `wr-stats`: first `.ttm-stats__label` font-size px(13).
- `wr-tiles`: padding-top px(16).
- `wr-books`: padding-top px(16).
- `wr-book-title`: add the first `.ttm-book__meta` margin-top px(2).
- New `wr-chapter-dek` (/writing/, 1280): first `.ttm-series-toc.is-chapters .ttm-numbered__dek` is 13px, neutral-800, margin-top 3px, font-weight 400; first `__date` is font-weight 400.

All phase-2 front-page rows, `404-latest` and `ar-mostread` stay green unchanged.
**Out of scope:** Any change to markup, render.php or templates. The base .ttm-cell-heading__link, the front page, and the 404 Latest list. The chapters row content (a separate task). Screenshots (the seed task regenerates them).
**Verification:** A Playwright computed-style probe at 1280 on the seeded site shows every value listed. The /signing-your-options-table/ gap between the first `pre` and the next paragraph is 22px. `npm run lint` passes (budget, coverage, stylelint). `npm run test:e2e` is green, including every phase-2 front-page row and selectors.spec. Full foundry_verify.
**Depends on:** none

### R3-02: Writing 'recent chapters' lists published chapters only
**Goal:** The `ttm/series-toc` chapters variant must list the newest published chapters, per mock 2d (12, 11, 10, 09) and 02 §D ('recent chapters … newest first, 4 items'). Today the scheduled Chapter 13 sits first on /writing/, beside a heading link that says 'All 12' (REVIEW round 2, finding 1).
**Files touched:** plugins/ttm-core/blocks/series-toc/render.php, tests/integration/Blocks/SeriesTocTest.php, tests/e2e/fidelity.spec.mjs
**Design constraints:** In render.php, when `$ttm_is_chapters`, filter `$ttm_rows` to parts whose status is 'publish' before the usort and array_slice (~:50-60). The `series` variant (the article TOC) keeps its scheduled rows and F24 behaviour unchanged. `$ttm_published` ('All N') is unchanged.

If no published chapter remains, the list is empty: the block returns '' and never renders an empty wrapper or placeholder (06 governing rule, rules 25 and 46).

Keep block wrapper shape and classes (rule 46). No nonces or per-visitor calls in render.php. No bare numeric literals beyond structural ones (rule 24).
**Acceptance tests:** Would have caught it:
- New `SeriesTocTest::test_chapters_variant_excludes_scheduled_parts`: a closed series (total > parts) with parts 1–3 published and part 4 `future`; variant chapters, order desc, limit 2. Assert the output contains '>03<' and '>02<', and does not contain '>04<' or part 4's title.
- Extend `test_chapters_variant_newest_first_limited_with_dek` or add a sibling that asserts the series variant still renders the scheduled part as is-scheduled.
- New fidelity row `wr-chapter-first` (/writing/, 1280): the first `.ttm-series-toc.is-chapters .ttm-numbered__title` is an `a` element with text 'Reconciliation', and the first `.ttm-numbered__num` text is '12'.

Confirm the integration test and the fidelity row fail before the render change.
**Out of scope:** The series (article TOC) variant, hub featured parts, series-single parts, seed dates, CSS.
**Verification:** curl /writing/ shows rows 12, 11, 10, 09 under 'The Quiet Ledger — recent chapters' with 'All 12'. The article TOC on /signing-your-options-table/ still shows parts 5 and 6 as scheduled (toc-scheduled row green). `npm run test:integration` and `npm run test:e2e` green. Full foundry_verify.
**Depends on:** none

### R3-03: Seed: Short fiction shows SPEC §6.10's four stories in mock order; no literal backticks; regenerate screenshots
**Goal:** Make /writing/'s Short fiction tiles show the four stories SPEC §6.10 and mock 2d name, in the mock's order. Today two non-fiction Writing essays are classed as stories and displace A Field Guide to Empty Offices and What the River Audits. Remove the markdown backticks that render literally in a seeded excerpt. Then regenerate the phase-3 PNGs so they reflect this round's code (REVIEW round 2, finding 2).
**Files touched:** docs/fixtures/seed/posts.json, plugins/ttm-core/src/Cli/Seeder.php, tests/integration/Cli/SeederTest.php, tests/unit/Cli/SeederTest.php, tests/e2e/fidelity.spec.mjs, docs/feedback/phase-3/writing.png, docs/feedback/phase-3/writing-390.png, docs/feedback/phase-3/article.png, docs/feedback/phase-3/article-390.png, docs/feedback/phase-3/article-1920.png, docs/feedback/phase-3/journal.png, docs/feedback/phase-3/journal-390.png, docs/feedback/phase-3/series-hub.png, docs/feedback/phase-3/series-single.png, docs/feedback/phase-3/archive-security.png, docs/feedback/phase-3/archive-390.png, docs/HANDOFF.md
**Design constraints:** Seed and Seeder only. The content model (Meta\Form::derive, Fiction\Serials::stories) is a SPEC §2 non-goal: do not change it.

(a) The two essays 'finishing-a-draft-you-no-longer-believe-in' and 'outlining-for-people-who-hate-outlines' (posts.json ~:459-479) must not have ttm_form 'story' after seeding. Add a documented posts.json fixture field (e.g. "form": "article") that the Seeder writes as ttm_form plus ttm_form_locked=1 after its existing re-derive passes. Validate the value against the allowed forms.
(b) Set `days_ago` for story-uptime and story-what-the-river-audits so Serials::stories(4) returns, newest first: The Last Cron Job (2026), A Field Guide to Empty Offices (2025), Uptime (2024), What the River Audits (2023). These are mock 2d lines 1157–1161.
(c) posts.json ~:501, Hardening WordPress part 1's excerpt: remove the markdown backticks (plain 'wp-config.php').

Requirements:
- No lorem (rule 39).
- Seed images per rule 45: Uptime keeps its featured image (F19 tile).
- The front page must not change: its 'Also running' story stays The Last Cron Job, and every phase-2 row stays green.
- Update the phase-2 test `test_seeded_writing_essays_derive_as_story_but_stay_older_than_the_last_cron_job` to the new truth: essays are not 'story', and stories(1) is still The Last Cron Job. Do not delete it.

After the fix tasks this depends on have landed, run `npm run env:cli -- ttm seed --reset` and `npm run screenshots`, then commit every changed PNG under docs/feedback/phase-3/. Update docs/HANDOFF.md: a Round 3 section plus the Field Guide days_ago noted in Measurements.

Never stage FOUNDRY_FEEDBACK.md. Restore ttm-theme if any test switches themes.
**Acceptance tests:** Would have caught it:
- New integration `SeederTest::test_writing_short_fiction_is_the_four_spec_stories_in_mock_order`: after seeding 'normal', map Serials::stories(4) to titles and assertSame ['The Last Cron Job', 'A Field Guide to Empty Offices', 'Uptime', 'What the River Audits'].
- New `SeederTest::test_no_seeded_excerpt_contains_a_backtick`: every seeded post's post_excerpt lacks '`'.
- If the Seeder's field parsing is pure, add a unit test for the new `form` field (valid value written and locked; invalid value ignored).
- New fidelity row `wr-tile-titles` (/writing/, 1280): the text tiles' `.ttm-tile__title` texts, together with the image tile's aria-label, equal the four titles in order.

Confirm the integration test and the fidelity row fail before the seed change.
**Out of scope:** Meta\Form, Fiction\Serials, story-tiles render.php, CSS, the phase-2 screenshot directory, any visual fix spotted in the PNGs (record it in HANDOFF instead).
**Verification:** curl /writing/ shows the four tiles in mock order. / still shows The Last Cron Job under Also running. `grep -n '`' docs/fixtures/seed/posts.json` finds nothing in excerpts. `grep -ri lorem docs/fixtures/seed/` is empty. `git merge-base --is-ancestor $(git log -1 --format=%H -- themes/ttm-theme plugins/ttm-core docs/fixtures/seed) $(git log -1 --format=%H -- docs/feedback/phase-3)` exits 0. `npm run test:integration` and `npm run test:e2e` green. Full foundry_verify.
**Depends on:** R3-01, R3-02
