# Phase 1 — proof-of-concept build (archived)

The first Foundry flight (2026-09-20/21) built `ttm-core` and `ttm-theme` from
`SPEC.md` in this directory. Everything here is historical: the plan, the task
log, the three review rounds, the handoff and the summary. The spec that drives
the current flight is `docs/SPEC.md`; where it and this directory disagree,
`docs/SPEC.md` wins.

Read `SUMMARY.md` for what was built and decided, and its "Spec issues" list
(SI-1..SI-13), which the phase 2 spec folds in.
