# Phase 2 screenshots

Written by `npm run screenshots` (SPEC §6.6) against the running, seeded wp-env site. Each
phase's push task re-runs it and commits the PNGs here so the owner compares against
`docs/feedback/design_*.png`.

| this phase | compare against |
|---|---|
| `masthead.png` + `lead-row.png` | `design_top.png` |
| `section-rows.png` + `series-strip.png` | `design_blocks.png` |
| `poster-footer.png` | `design_footer.png` |

`front-1280.png` and `front-390.png` are the full front page at each viewport, for context —
they have no single `design_*.png` counterpart.
