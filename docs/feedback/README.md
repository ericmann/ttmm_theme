# Owner feedback screenshots

`design_*.png` — the Claude Design mock `2a` rendered at 1280 (target).
`preview_*.png` — the phase 1 build on `localhost:8888` at the same crops (2026-09-21).
`phase-2/` — the seven PNGs `npm run screenshots` writes from the phase 2 build (SPEC §6.6); see `phase-2/README.md` for which crop compares against which `design_*.png`.

| crop | design | preview |
|---|---|---|
| masthead + lead row | `design_top.png` | `preview_top.png` |
| section rows + series strip | `design_blocks.png` | `preview_blocks.png` |
| poster + footer | `design_footer.png` | `preview_footer.png` |
